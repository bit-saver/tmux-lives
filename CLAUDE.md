# tmux-lives — fish plugin

**tmux-lives** is the tmux automation system (categorized sessions, switcher, persistence,
ShellFish coexistence) extracted from `~/.config/fish` into a standalone, cross-platform
**fisher plugin** (Linux now; macOS = spec 2).

**Status (2026-06-23 / 2026-06-30): SHIPPED + LIVE on the Linux host; Feature 2 (in-tmux command surface) SHIPPED, then its `M-m` modal was REDESIGNED into a single-shot launcher + native `M-r` scratch-resize mode (feat/modal-redesign, opus-approved, merging to main).** spec-1 extraction done + unified command refactor done. The **popup switcher** is the live switcher — `prefix S` and `Opt+s` (`bind -n M-s`) open a pure-fish two-pane `display-popup`: categorized list (claude/running/general) with `╭──`/`│` category-colored border, `▐` selected block, muted-yellow `❯` current marker, live `capture-pane` preview. **Keys:** `↑↓`/`j`/`k` move · `Enter` switch · `x` kill highlighted session (`kill <name>? (y/n)` confirm → kill + refresh) · `Esc`/`q` cancel. `display-menu` is the no-`display-popup` fallback. 8 suites pass: `for t in tests/test-*.fish; fish $t; end`. All functionality is under `tmux-lives <verb>` — top-level (help-page order): meta cluster `help` · `setup` · `update` then session cluster `new/attach/picker/fix/categorize/clear/close` (short aliases `u`=update, `n/a/p/f/c/x|q`; `fix` was renamed from `fixssh`); **`categorize`/`c`** re-runs `__tmux_categorize` to fix a mis-named session (e.g. macOS version-named claude); **hidden shortcut**: the setup subcommands (`install/i`, `verify/v`, `teardown`, `keys`, `auto`) also work at top level (`tmux-lives auto on` == `tmux-lives setup auto on`) — kept OUT of the help on purpose; **`new`/`n`** no-name path is hardened (guards an empty `#{session_id}` before `switch-client`); setup group: `setup install/verify/teardown/keys/auto` (old `start` and `take` removed; `verify`/`teardown`/key-flags/`auto` nested under `setup`). Help page uses **alias-first columns** (`__tmux_lives_help_lines` renders each row with `printf '%-2s%-28s%s'` = shortcut · command+args · description) so the short forms line up in a left rail; the `help` row was removed (redundant — you already found it). `close` shows only `x` in the help though `q` still routes. Meta cluster is `setup` then `update`, then the session cluster `new/attach/picker/fix/categorize/clear/close`. Both `tmux-lives help` and `tmux-lives setup -h` are framed in a rounded, orange (256-color 208) box via **`__tmux_lives_box`** (title in the top edge, frame orange + title default-fg); content lives in `__tmux_lives_help_lines`/`__tmux_lives_setup_help_lines` (kept separate from the frame so ordering/content is testable on the unframed text). Setup-help descriptions were tightened so the framed page fits 80 cols (was up to 104). NB for `__tmux_lives_box`: it measures width with `string length --visible` (display columns) and pads via a **quoted** var — an inline `(string repeat -n 0 …)` expands to ZERO args and shifts the trailing printf fields (silently broke the colored right border on max-width rows). **`update`/`u`** wraps `fisher update bit-saver/tmux-lives` and reports whether anything changed — it cksum-digests the installed files before/after and prints "already up to date" vs "updated — exec fish", setting a `_tmux_lives_updating` flag that silences the generic post-update event note so there's no double/contradictory message (direct `fisher update` still gets the generic note). fisher always reinstalls (re-`curl`s the HEAD tarball — no version check; confirmed in fisher 4.4.8 source), so `update` **diverts fisher's noisy output to a temp file (a `>file` redirect, NOT a `(…)` capture — that would break fisher's background-job fetch) and only surfaces it when the files changed or fisher failed** — a no-op update is one quiet line. `tmux-lives setup install` wires `~/.tmux.conf` + TPM plugins; `tmux-lives setup keys [-p/-s]` configures key bindings; `fisher install`/`update` print post-install/update guidance.

### Live wiring (the cutover, done 2026-06-19 — REAL fisher install)
- Installed via `fisher install bit-saver/tmux-lives` → files live at `~/.config/fish/{conf.d/tmux.fish,
  conf.d/tmux-lives-install.fish, functions/tmux-categorize.fish}`, tracked in `fish_plugins` + `_fisher_plugins`.
- `conf.d/tmux.fish` resolves the categorizer via `$__fish_config_dir/functions/tmux-categorize.fish` (portable).
- `~/.tmux.conf` **sources the managed fragment** — its last lines are `source-file ~/.config/tmux/tmux-lives.conf`
  then `run '.../tpm/tpm'`. So all the tmux-lives wiring (categorize tick, switcher binds, the ShellFish commandeer
  + `client-attached` hooks, LC_TERMINAL passthrough, and the resurrect/continuum `@plugin` declarations + options)
  lives in the **rendered fragment** and is `tmux-lives setup`-managed — NOT hand-edited and NOT hardcoded in
  `~/.tmux.conf`. The user only hand-maintains the bare TPM line + `tmux-sensible`/`tmux-yank` there (and never ran
  the full `setup install`, which would also add systemd units). **Getting new fragment wiring live = `fisher update`
  then any `setup` action** (or just `fisher update`: the `_tmux_lives_post_update` handler now re-renders the
  fragment when one exists). `tmux-lives setup install` is the from-scratch path (e.g. the Mac).
- **Dev loop from here:** edit → `for t in tests/test-*.fish; fish $t; end` → commit + push. **Stop there.**
  Deployment to the live `~/.config/fish/` is **ALWAYS the user's `fisher update`** — they run it themselves in
  their interactive fish.
- 🚫 **A Claude session does NOT deploy.** Finished changes reach the live `~/.config/fish/` ONLY via the user's
  `fisher update` — never `cp` a finished change into `conf.d/`/`functions/` to make it "live," and don't edit
  `~/.tmux.conf` or set universal vars to ship something. (`fisher install/update` also HANGS in the Claude bash
  sandbox anyway — but the rule stands regardless: the user always deploys.) If a change "needs to be live to
  verify," push it and ask the user to `fisher update`.
- ✅ **Temporary test edits are allowed — but revert EXACTLY.** You may patch a live fisher file to observe a
  behavior in development, on one strict condition: restore it **byte-identical** to its fisher-install state
  afterward and confirm with a diff. Clean restore: `git show <installed-commit>:<path> > <live-path>` then
  `diff` to verify zero drift. Leaving the live copy desynced from what fisher installed is the thing to avoid
  (fisher treats its files as static; drift makes it unclear which copy is authoritative).

**macOS port (spec 2): implemented** — runtime-only persistence (no launchd units; continuum autosave + first-access restore), `/proc`→`ps` detection (`__tcz_pid_comm`/`__tcz_pid_cmdline`), bare cold-start on first attach. **Pending:** live Mac smoke (categorize as claude, cold-start, reboot-restore). **macOS GUI-namespace — investigated 2026-07-13, NOT built (see `docs/macos-gui-namespace-findings.md`):** the `HANDOFF-macos-gui-namespace.md` premise doesn't hold on macOS 26.5.2 — `reattach-to-user-namespace` as `default-command` is a proven **no-op** (it re-parents the Mach bootstrap subset but leaves the **audit session** unchanged, and GUI window/menu-bar placement is governed by the Aqua audit session `asid`, not the bootstrap domain); `pbcopy`/`pbpaste` and `open -a` already work. The only real gap (a GUI **binary run directly** from a tmux pane inherits the server's non-Aqua session) has a trivial `open -a`/`open <App>.app` workaround; the only true fix (a `gui/502` LaunchAgent to Aqua-root the server) was rejected as a disproportionate reversal of the "runtime-only, no launchd units" design. Don't re-chase reattach. The original trigger (pingy-mac's menu-bar icon) runs in Aqua correctly — its issue is its own `NSStatusItem`/window-frame logic, handed to the pingy-mac session. On the Mac: `fisher install bit-saver/tmux-lives` + run `tmux-lives setup install` (binds `prefix S` + `Opt+s` by default; use `tmux-lives setup keys -p/-s` to customize). **ShellFish bar color + non-ShellFish baseline (2026-06-27):** a `client-attached` hook in the managed fragment calls the categorizer subcommand `on-attach <client_pid> <client_tty> <color>` (`__tcz_on_attach` in `functions/tmux-categorize.fish`); detection reads the attaching client's process environ (`/proc/<pid>/environ` on Linux, `ps eww` on macOS) via `__tcz_pid_environ` / `__tcz_client_is_shellfish`. ShellFish clients get the per-server bar color emitted as an OSC escape (`__tcz_emit_barcolor`) directly to `#{client_tty}` — only that ShellFish tab sees it; non-ShellFish clients trigger `tmux source-file ~/.tmux-lives.conf` (if present) via `__tmux_lives_baseline_path` / `__tmux_lives_seed_baseline` to re-apply the user's own settings so ShellFish's forced options don't leak. Two config surfaces: `setup color <css>` (`__tmux_lives_color_cmd`) stores the color as the universal var `tmux_lives_bar_color`, baked into the managed fragment on every install/update — also derives the global `status-style` via `__tmux_lives_derive_status`: the bar **bg** is the ShellFish color lightened (`c+(255-c)×0.25`, default) or darkened (`c×0.75`, with `-i`/`--invert`, persisted as `tmux_lives_status_invert`), and the text **fg** is a hex tint of that derived bar at f=0.68 (toward white on dark bars / black on light, by luminance) — true-hex/palette-independent, replacing the old named black/white that rendered "tan"; baked into the fragment; hex/`rgb()` only — named colors and `color(p3 …)` skip gracefully; the status bar was previously the unclaimed tmux default green); `setup conf [edit|add <cmd>|reset]` (`__tmux_lives_conf_cmd`) manages user-owned `~/.tmux-lives.conf` — now the general tmux-lives config: sourced by the managed fragment at load (`if-shell '[ -f … ]' 'source-file …'`) + re-applied on non-SF attach; seeded via `__tmux_lives_baseline_template` (12h month-first clock in `@tmux_lives_status_right` + `status-right-length`; the old `status-left`/window-status layout was moved OUT to the fragment's `status-format[0]` — see the status-bar overhaul below); `status-right` in the fragment is `#{T:@tmux_lives_status_right}#(tick)` — the `T:` modifier makes strftime reach the user-set `@var`. **NB the old claim here ("the file never sets `status-right` so a re-source can't wipe the tick or continuum") was TRUE of the user's `~/.tmux-lives.conf` and FALSE of the fragment, which DID assign it — see the status-right merge fix below;** `setup conf reset` backs up to `.bak` and restores defaults; **never overwritten** by normal `conf` commands. **Status-bar controls (2026-07-02):** `C-M-a` toggles `status-position` top/bottom and `C-M-s` toggles `status` on/off, via categorizer verbs `status-pos-toggle`/`status-vis-toggle` (`__tcz_status_pos_toggle`/`__tcz_status_vis_toggle`) that flip the live option then persist BOTH via `__tcz_write_state` to a **machine-owned** `~/.config/tmux/tmux-lives-state.conf` (seam `tmux_lives_state_file`, install-side path `__tmux_lives_state_path`); the managed fragment sources that file with `if-shell '[ -f … ]' 'source-file …'` **after** the status-style + baseline setup so a persisted position/visibility wins, reapplying it on every server start (survives reboot). The keys are configurable: `setup keys --status-pos-key`/`--status-vis-key` (universals `tmux_lives_status_pos_key`/`tmux_lives_status_vis_key`, defaults `C-M-a`/`C-M-s`, `''` disables), baked into the fragment (argv[9]/argv[10] of `__tmux_lives_render_fragment`). NB the user already sets `status-position` (top) somewhere in their own config — once the toggle owns it, any manual `status-position` line should be dropped to avoid a source-order tug-of-war. **`setup color --apply`/`-a`** (`__tmux_lives_color_cmd`) reapplies the currently-stored `tmux_lives_bar_color` live to BOTH surfaces — the tmux `status-style` (set directly via the `tmux_lives_tmux_socket` seam, no fragment re-render/reload) and the ShellFish tab OSC (`__tcz_recolor`) — handy when a new ShellFish tab (Cmd+T) comes up without the color; it takes no color argument (mutually exclusive with a positional css). The status-right categorize tick re-checks the ShellFish bar color + tab title on every `status-interval` — but as of the **reactive-emission fix (2026-07-11)** it emits **only when the value changed for that tty** (`__tcz_recolor`/`__tcz_retitle` take a `dedup` mode; the tick passes it — `__tcz_recolor $argv[2] dedup` / `__tcz_retitle dedup`; discrete events — `client-attached`/`client-session-changed`/`setup color` — still FORCE-emit and refresh a per-tty cache in `@tmux_lives_emit_<sanitized-tty>_{title,color}` global options via `__tcz_emit_key`/`_get`/`_set`). This killed a ShellFish **cursor flicker**: the old tick wrote OSC 2 (title) + OSC 6 (bar color) to the tty every cycle *unconditionally* and ShellFish repainted the cursor each time (bisected: `status off` OR a plain `status-right '%H:%M:%S'` both stop it → the OSC writes, not the redraw). A tab that silently dropped its color with no re-attach (iOS suspend/resume, mosh reconnect — no `client-attached` fires) is caught by a **color-only backstop** re-emit every `@tmux_lives_heal_interval` s (default 120, `0` disables; next-emit epoch in `@tmux_lives_heal_at`, gated by `__tcz_heal_due`). The fragment still bakes the color into the tick call (`#(… tick '<color>')`, re-baked on every `setup color`); `setup color --apply` remains a manual override. **NB the empty-cache dedup gotcha (review-caught):** `test "$x" = (__tcz_emit_get …)` THROWS (fish zero-word command substitution → "Missing argument", a stderr stack trace that self-heals but isn't pristine) when nothing is cached yet — capture into a var and quote: `set -l cached (…); test "$x" = "$cached"`. The left-hand `claude` window name renders in `@tmux_lives_claude_color` (default Claude coral `#D97757`, static/independent of the ShellFish-derived bar color) via a `window-status-format` conditional (`#{?#{==:#{window_name},claude},#[fg=…]#W#[fg=default],#W}`). **ShellFish cursor-flicker fix + categorize dedup (2026-07-12, feat/shellfish-cursor-and-cleanup):** ShellFish "blinks the cursor like crazy" on every tmux redraw because tmux's `cstyle` terminal-feature makes it RE-EMIT the cursor style on each redraw (ShellFish resets the cursor each time; the bare shell is steady). Fix = pin a STEADY `cursor-style` (re-emitting "steady" is invisible): the fragment seeds `set -g cursor-style <style>` from universal `tmux_lives_cursor_style` (render argv[11], default `block`, `''` = leave tmux's default). Also `__tcz_set_claude_opt` now DEDUPS — only `set-option @tmux_lives_claude` when the value changed (capture+quote the current, empty-cache gotcha) — because the unconditional per-tick / `fish_postexec` set forced needless bar redraws (each redraw = a cursor re-emit = a flicker). The earlier OSC-emit dedup (`ebc8a36`) and automatic-rename were NOT the flicker; ANY redraw triggers it. See [[shellfish-cursor-flicker]]. ShellFish tabs also get a per-tab TITLE via OSC 2 written straight to the client tty (`__tcz_emit_title`, mirroring `__tcz_emit_barcolor`): `<host>: <dir> [(C)]` — `hostname -s` (seam `tmux_lives_hostname`), the client session's active-pane dir basename (`$HOME`→`~`), and ` (C)` when any pane runs claude. `__tcz_retitle` iterates `list-clients` (carrying `#{client_session}`) and titles each ShellFish client from its own session; wired on `client-attached` (via `__tcz_on_attach`), a `client-session-changed` fragment hook, and the ~15s tick (`retitle` verb). A session may set `@tmux_lives_name "<name>"`; the categorizer then shows `<name>` as its display (switcher + overview) WITHOUT renaming the tmux session (the slug is left alone), and the ShellFish tab title (`__tcz_session_title`) uses `<name>` instead of the dir basename. Generic pager/tailer commands (`$__tcz_boring`: tail/less/watch/cat/more/bat) no longer name a session — they fall back to the directory. Test seams: `tmux_lives_fake_environ` (inject a fake client environ), `tmux_lives_baseline_conf` (override the baseline path), `tmux_lives_state_file` (override the status-toggle state path), `tmux_lives_tmux_socket` (pin install-side live-tmux calls to a `-L` socket).

**Status-bar overhaul (2026-07-10, feat/status-bar-overhaul → main):** the managed fragment now OWNS the bar via `set -g status-format[0]` (pure builder `__tcz_status_format` in `functions/tmux-categorize.fish`, echoed at render time through the categorizer verb `status-format`). Three `#[align=left/centre/right]` zones: **ends-only powerline caps** (slants `\U0000e0b0`/`\U0000e0b2`) — host cap far-left (`#{host_short}` + a per-host glyph: cod-remote `\U0000eb3a` remote / cod-vm `\U0000ea7a` local, chosen by `@tmux_lives_host_kind`), clock cap far-right; **flat middle** with the window list on the LEFT (names-only, `•` sep, current bold — `window-status-format='#W'`/`-current-format='#[bold]#W#[nobold]'`/`-separator=' • '`) and the session/Claude identity **dead-center** (`@tmux_lives_name`|`session_name`, then `✦ #{@tmux_lives_claude}`). Separator roles: `✦`=Claude mark, `•`=between windows, `·`=between fields. The **right zone renders `status-right`** via `#{T;=/#{status-right-length}:status-right}` so the invisible `#(tick)` self-heal + continuum autosave survive untouched; `status-style` (ShellFish color) unchanged; the state-file `if-shell` (position/visibility) still sources AFTER the new block. **Mode indicators** (client-scoped): prefix → caps glow + `❯` center (`#{?client_prefix,…}`); `M-r` resize → amber caps + persistent `◇ RESIZE ◇` (`#{?#{==:#{client_key_table},tmuxlives-resize},…}`). All tunables are live tmux **`@options`** seeded by the fragment — `@tmux_lives_cap_bg` (from the ShellFish-derived bar bg via new helper `__tmux_lives_derive_status_bg`), `_cap_fg`, `_prefix_color`, `_resize_color`, `_glyph_remote`, `_glyph_local`, `_host_kind`, `_claude` — retune with `tmux set -g @… …`, no re-render (accents default amber; user judges live). `host-kind`=`__tcz_host_kind` (universal `tmux_lives_host_kind` override → SSH-env → local); `@tmux_lives_claude` populated per session by `__tcz_set_claude_opt` on the categorize/tick pass (BARE-name `set-option`, [[tmux-target-quirks]]; empty when no claude pane, so no stale `✦`). **Identity source fixed 2026-07-26 @ `2b5e393`:** `__tcz_set_claude_opt` sourced `@tmux_lives_claude` ONLY from the `--name` flag (`__tcz_cmdline_name`), but claude is normally started WITHOUT it (`claude --enable-auto-mode -c`) — so the option stayed empty and `__tcz_status_identity` fell back to `#{session_name}`, i.e. a SLUG (spaces→dashes) that is ALSO frozen for an unstamped restored claude breadcrumb (`conf.d/tmux.fish:100-104` leaves those unstamped ON PURPOSE so the guard preserves their meaningful name — side effect: `__tcz_owned` is false forever, so the slug never re-tracks the title). Both symptoms (`TMUX-Setup-18` for a live `TMUX Setup 21`) were that ONE cause. Fix: fall back to `__tcz_title_name` on the pane title — the same source `__tcz_categorize:340` already slugifies for the session name — with `--name` still winning and an unparseable title still ignored; `list-panes` format gained `#{pane_title}` and the split became `-m 2` (title last, may contain tabs; mirrors `__tcz_snapshot`'s `-m 4`). **WATCH (reviewer-flagged, live-only):** the title is a far more volatile source than a CLI flag, and `__tcz_set_claude_opt` runs on the ~15s tick AND every `fish_postexec`. `__tcz_title_name` already absorbs leading-glyph churn and a trailing ` - <action>` tail (both test-covered), but a title that puts transient state MID-string (e.g. `⠂ Editing foo.py` replacing the name) would flap the option, defeat the dedup, and re-open the [[shellfish-cursor-flicker]] class of bug. Not observed; verify during an ACTIVE claude turn. **NUMERIC-SESSION TARGETING FIXED 2026-07-26 @ `ea15699`/`3b4d306`/`8abe0b8`:** in tmux 3.3a a PURELY NUMERIC session name — which is what EVERY freshly created session has — resolves in `-t` as the CURRENT session, ignoring the name, and **`=` does NOT rescue it** (with `"0"` running sleep + `neighbour`, `list-panes -t "=0"` returned NEIGHBOUR's panes; `display-message -p -t "=0"` returned empty; only `-t '$id'` was right). Two real bugs, both reproduced: the `@tmux_lives_name` claimed-lookup read a DIFFERENT session's claim, so when that one was claimed (neurotto sets one) the numeric session was skipped → never renamed/stamped → **PERMANENTLY stranded at its numeric name**; and `__tcz_set_claude_opt` read/wrote the wrong session's `@tmux_lives_claude` while its `list-panes` hit the wrong panes. Also hit `__tcz_pick_general` (a BUSY numeric session looked idle → commandeer could bounce a fresh tab onto real work) and `__tcz_session_has_claude`/`__tcz_session_title` (numeric `#{client_session}` → a neighbour's dir on a ShellFish tab). Fix = **two** helpers, because the command families want different shapes: **`__tcz_session_target`** → `$id` if numeric else the BARE name (for `set-option`/`show-option`, which reject `=`, AND for **`capture-pane`, which also rejects `=name`** — `can't find pane: =alpha`); **`__tcz_pane_target`** → `$id` if numeric else `=name` (for `list-panes`). Both cost ZERO tmux calls for non-numeric names (they run per session per tick). Routing `capture-pane` through the pane shape blanked the picker preview for every non-numeric session — caught in re-review; the old source-grep guard passed VACUOUSLY once the `=` came from a helper, so there is now an end-to-end preview test. Unaffected (verified with real pty clients): `switch-client`/`detach-client -s`/`list-clients`/`has-session`; `__tcz_commandeer` is safe by construction. Residual, documented not fixed: tmux reads a `$<digits>`-shaped target as an ID even with `=`, so a session literally NAMED `"$1"` is unaddressable. Harness note: a `-L` socket still loads `~/.tmux.conf` unless started `-f /dev/null`. See [[tmux-target-quirks]].. The user baseline `~/.tmux-lives.conf` no longer owns layout (old `status-left`/window lines become harmless no-ops for existing installs — the fragment wins). **Two gotchas baked in (both were latent bugs in the plan's own code, caught in review):** color `@options` are single-quoted — an unquoted `#hex` value is a tmux COMMENT → the option silently goes empty and `source-file` still returns rc0 (so a plain parse test misses it); and each render-time `fish --no-config $cat <verb>` output is computed into a var BEFORE `set -a f "…"` — a string concatenated with a zero-output command substitution collapses the whole `set -a` argument to an empty list, so the entire option line VANISHES (same zero-arg hazard as the `__tmux_lives_box` note above). Tests: pure builder + `__tcz_host_kind` + `__tcz_set_claude_opt` (stub) in `test-tmux-categorize.fish`; fragment-render + a real-categorizer-path `-L`-socket `source-file` parse + a live `show -gv @tmux_lives_cap_bg` non-empty assertion in `test-tmux-install.fish`. **Pending live smoke** (needs an attached Nerd-Font client — deferred, not automatable): powerline caps, centered zones, glyph rendering, prefix/resize recolor + `◇ RESIZE ◇` clear-on-exit, and a `tmux set -g @tmux_lives_prefix_color …` live retune. **Adaptive cap color + date-first clock (2026-07-11, feat/adaptive-cap-color):** the caps didn't pop because `@tmux_lives_cap_bg` came out IDENTICAL to the bar bg (both were `__tmux_lives_derive_status_bg` of the ShellFish color) — no contrasting segment — and the slant tapered to `bg=default` (a notch). Fixed: new `__tmux_lives_derive_cap_bg <bar_hex>` derives the cap as a **luminance-adaptive shade of the bar** (lighter on a dark bar / darker on a light bar; threshold `L>140`, same `+（255-c)×0.25` lighten / `×0.75` darken as `derive_status`); the fragment now seeds `@tmux_lives_bar_bg` (= the bar bg) alongside `@tmux_lives_cap_bg` (= `__tmux_lives_derive_cap_bg` of bar_bg), and BOTH powerline slants taper `bg=#{@tmux_lives_bar_bg}` (cap→bar, no notch). Baseline clock default swapped to **date-first** (`@tmux_lives_status_right "%b %-d · %-I:%M %p"`) — NB existing users' `~/.tmux-lives.conf` keeps the OLD order until they update it themselves (it's user-owned; `setup conf edit` or a live `tmux set -g @tmux_lives_status_right`).

**Feature 2 — in-tmux command surface (2026-06-30, feat/in-tmux-command-surface → main):** drives tmux-lives from inside a pane occupied by a full-screen program. **Modal → single-shot launcher** (`M-m` default, `bind -n M-m`; REDESIGNED — see the modal-redesign paragraph below): `__tcz_modal` draws the design-B legend (`__tcz_modal_legend`) once, reads ONE key (`__tcz_modal_readkey`), maps it (`__tcz_modal_action`), then the popup CLOSES and `__tcz_modal_run` runs the action visibly (no popup-in-popup): `p` picker (deferred `run-shell -b … open-switcher`) · `n` new · `c` clear · `g` categorize · `t` scratch toggle · `r` resize (enters the `M-r` mode) · `b` bar color (inline cooked `read`) · `esc`/`q` close; `display-menu` fallback via `__tcz_modal_menu` / `__tcz_modal_menu_args`. **Scratch split** (`M-t` default, `bind -n M-t`): `__tcz_scratch` (toggle; with `__tcz_scratch_pane` to find the marked pane and `__tcz_scratch_orient` for `h`/`w`) — splits a throwaway shell pane beside the active pane, tagged with the pane option `@tmux_lives_scratch` (origin recorded in `@tmux_lives_scratch_origin`); re-press refocuses the original pane and kills the scratch. **Colored preview**: switcher's right pane now uses `capture-pane -e` + `__tcz_popup_truncate`/`__tcz_popup_clip` (ANSI-aware truncation/clip) so the preview shows real session colors. **`setup color` robustness**: bare hex like `1f6feb` is normalized to `#1f6feb` before storage; `setup color` now calls `__tcz_recolor` (new categorizer subcommand) to re-emit the OSC bar-color escape directly to all attached ShellFish clients immediately — no reattach needed. **Fragment binds + config**: `setup keys --modal-key <k>` / `--scratch-key <k>` store universal vars `tmux_lives_modal_key` / `tmux_lives_scratch_key`, baked into the managed fragment on render; `''` disables the bind; defaults `M-m`/`M-t`; consistent with existing `-p`/`-s` flags. **Test-isolation fix**: the install suite previously called the real `_tmux_lives_post_update` → `__tmux_lives_write_fragment`, mutating the live fragment and server on every test run; now stubbed so post-update note tests run without side effects. **Pending live smoke**: raw-tty key loop, cooked `read` color input inside the popup, live resize-under-popup, and key-collision verification are runtime-only — not covered by the unit suite.

**Modal REDESIGN — SHIPPED (2026-06-30, feat/modal-redesign → main; 7 TDD tasks, opus final review = READY TO MERGE):** live use found the old looping `M-m` modal's actions couldn't run *inside* a popup overlay (tmux blocks popup-in-popup → the picker silently died; scratch/session effects hid behind the popup). Fixed by making `M-m` a **single-shot launcher** (`__tcz_modal`: draw legend → read ONE key → popup CLOSES → `__tcz_modal_run` runs the action visibly; the one popup-shaped action, the picker, is deferred via `tmux run-shell -b … open-switcher` so it opens after the launcher closes — no nesting), and moving live scratch resizing to a **native `M-r` key-table** (`tmuxlives-resize`, panes stay visible): arrows resize the marked scratch (`__tcz_scratch_resize`, 4 cols / 2 rows), `h`/`w` orient (`__tcz_scratch_orient`), `x` kill (`scratch-kill`, guarded kill-only), `Escape`/`Enter` exit; entered by the `M-r` bind or the launcher `r` key via `__tcz_resize_enter` (shows a `display-message -d 0` hint; nudges if no scratch exists). New categorizer verbs: `scratch-resize` / `resize-enter` / `scratch-orient` / `scratch-kill` (dispatched in `__tcz_main`). Legend is **design-B** (`__tcz_modal_legend`: categorized session/scratch/config headers in 208/cyan/green + a two-column keybind table of the *effective* binds). Config: `setup keys --resize-key <k>` (universal `tmux_lives_resize_key`, default `M-r`, `''` disables), baked into the managed fragment; the modal `display-popup` bind now passes the effective `M-m/M-t/M-r/M-s` keys to `__tcz_modal` so the legend + keys-table reflect them. User-facing **switcher→picker** rename (the internal categorizer verb `open-switcher` and `__tcz_open_switcher`/`__tcz_popup` are unchanged — only labels say "picker"). Spec `docs/superpowers/specs/2026-06-30-modal-redesign-design.md`, plan `docs/superpowers/plans/2026-06-30-modal-redesign.md`. **Pending live smoke** (runtime-only, user-validated after `tl update`): the raw-tty single-key read + inline cooked color `read` inside `display-popup -E`, the deferred `run-shell -b` picker open, the live `M-r` mode (`switch-client -T tmuxlives-resize`, sticky re-enter, the `-d 0` hint), and `M-m/M-t/M-r` key collisions.

**Cap-color formulas (2026-07-12, feat/cap-color-formulas → main @ 404e2a4; 4 TDD + opus final review):** the powerline cap derives its color from the bar via a chosen color-theory formula, stored in universal `tmux_lives_cap` (default `mono`). Surfaces: `tmux-lives setup cap <formula>|list` CLI (`__tmux_lives_cap_cmd`) + an interactive **`cap-picker`** popup (categorizer verb; ↑↓ move, ←→ flip ± direction, Enter apply). Tokens: `mono`/`complementary`/`analogous±`/`split±`/`triadic±` + literal `#hex`. Picker runs under `fish --no-config` so it batch-computes swatches via one config-loaded `fish -c` and applies through the CLI (single source of truth); apply-live uses the `tmux_lives_tmux_socket` seam; `set -U` tests save/clear/restore the universal (no leak). **NB the v1 derivation (naive HSL hue-rotation) is being REPLACED:** a full **OKLCH perceptual palette redesign** is spec'd+planned on branch `feat/cap-color-oklch` (spec `8be66ee`, plan `39ce368`; NOT built — Phase-1 subagent-driven build queued for a fresh session). v2 = decouple channels (only HUE from the bar; chroma/lightness are fixed role targets → a value-structured palette `bg/dim/muted/accent/text`), **RYB-default wheel** (green→warm, not muddy purple), WCAG contrast fg, 12-iter gamut-clamp; adds `--vividness`/`--wheel`, an `M-m` `k` cap-color entry, a framed picker (fixes v1's borderless blend), and a `<token>`→`<formula>` rename. Phase 2 = whole-bar theming (palette across ✦/mode/session/window). Full OKLCH pipeline validated bit-exact in fish (proto at `~/.claude/session-data/2026-07-12-oklch-proto.fish`). See memory [[cap-color-formulas]]. **UPDATE 2026-07-13 — v2 OKLCH engine Phase 1 SHIPPED to main @ `a47eda0`** (6 TDD subagent-driven + opus review + cleanup; perceptual OKLCH palette `bg/dim/muted/accent/text`, RYB default, WCAG fg, 12-iter gamut-clamp; `setup cap <formula>|list|--vividness|--wheel`, framed picker w/ ←→/v/w, `M-m k` entry, `<token>`→`<formula>` rename; the entire v1 HSL cap cluster — `cap_from_formula`/`cap_hue`/`derive_cap_bg`/`hue2rgb` — removed). **Then Phase A picker polish SHIPPED @ `b0d72c8`** (4 TDD + opus review): restore-last-selection-on-open (pure `__tcz_cap_restore`), fully-bordered picker with a `├─┤` separator (pure `__tcz_cap_sep`), a dedicated **`M-k`** cap-picker keybind (`setup keys --cap-key`, universal `tmux_lives_cap_key`, render argv[15]), and the scratch split widened 33%→45% (`__tcz_scratch`/`_orient`). **Cap role is user-selectable (v2):** the powerline cap renders from a chosen palette column — `dim`/`muted`/`accent` (default accent) — via `←→` in the picker or `setup cap --role <r>`, stored in universal `tmux_lives_cap_role` and plumbed through fragment argv[16]; `text` remains computed-but-unused and whole-bar theming is still Phase B. **Cap-picker v2 SHIPPED to main @ `853f30c`** (2026-07-13, `feat/cap-picker-v2`, 7 TDD tasks subagent-driven + two opus reviews clean + review polish): the **tl theme palette** (reusable `__tcz_theme <role>` accessor — brand `#ff8a1f`/border `#a86a2c`/key `#f5cf8a`/muted `#9a8a72`/value `#6fc7b8`/gray sel-bg), a **flat 10-scheme list** picker (adds `square`; `tetradic`/`square` share an accent but differ in muted — intentional) with exact-index restore-on-open, **`←→` cap-role shift**, a redesigned frame (title · primary cluster · `d m a` header + right-aligned legend · 10 swatch rows · footer · status), the popup grown 34×15→44×22 at all 3 open sites (`__tmux_lives_cap_picker`, the `M-k` fragment bind, `__tcz_modal_run` `k` — modal launcher left at 34×15), and the **`formula`→`scheme`** rename completed everywhere (CLI strings + descriptions; the two `__tmux_lives_hsl_*` "standard formula" descriptions name the HSL math, not the token, left as-is). Review polish folded in: setup-help `cap` row now enumerates `scheme/vividness/wheel/role` (re-padded to fix the rename's 1-col misalignment), and the picker's enter-apply redirects stdout (`>/dev/null 2>&1`) so the CLI's 4 per-flag confirmation lines don't flash as `display-popup -E` closes. 8 suites green (install 329). **Phase B (whole-bar theming — apply dim/muted/text + bar color across ✦/mode/session/window, meaningful picker column labels, set-primary/bar-color IN the picker, retrofit modal/switcher menus to the tl palette; opt-in `tmux_lives_theme_bar`) NOT started.** All v2 visuals (picker draw, `←→`/`M-k`/`M-m k` popups, live role-shift, `square`) are runtime-only — **pending the user's live smoke after `fisher update`**; one deferred cosmetic (the selected-row highlight spans content-width, not full inner width to the right border — user to judge live). **Marker/frame fixes SHIPPED to main @ `7975669`** (2026-07-14, `fix/cap-picker-marker`, spec `docs/superpowers/specs/2026-07-14-cap-picker-marker-design.md`) from the v2 live smoke — three defects, two of them v2 regressions: (1) **top border was scrolling off** — the frame is exactly 22 rows and the draw gave EVERY row a trailing `\n`, so the last one put the cursor on row 23 of the `-h 22` popup → terminal scrolls → row 1 eaten; fixed by emitting rows `1..-2` with `\n` and the final row without (popup geometry UNCHANGED at 44×22 — the frame now hugs it exactly; guard the `count -gt 1` or a zero-element `printf '%s\n'` emits a spurious blank line). (2) **`d m a` header misaligned, and the misalignment moved with `←→`** — header was letter-pitch 3 (`d  m  a`) against 2-wide adjacent cells (pitch 2; only `m` ever landed), AND `__tcz_cap_swatch_line` **prepended** `▎` to the active cell, INSERTING a column that slid the cells out from under the static header; fixed to pitch 2 + a 2-space lead (letters at inner cols 3/5/7 over cells at 3-4/5-6/7-8) and extracted to a pure **`__tcz_cap_dma <activecol>`** so the alignment is unit-testable (inline draw code wasn't). (3) **marker jarring + same color as the `▐` selector** — dropped the per-row `▎` entirely (it was half of bug 2) for **C2**: underline (SGR 4) the active cell on the **selected row only**, in a new neutral-gray **`mark`** theme role (`#8a8a8a`, distinct from tan `key` and the WARM `muted`); non-selected rows are an unbroken color band, and the primary cluster already previews the cursor row's scheme×role so marking other rows bought nothing. Rows are now a FIXED 22 visible cols for every activecol + both selection states (was 23 + shifting). NB **SGR 0 cancels SGR 4**, and the selected-row SELBG re-assertion inserts a bg-only SGR, so the underline can't bleed past its cell (review-verified). Also fixed a **pre-existing test-isolation leak**: `tmux_lives_cap_role`'s save/clear guard sat around the `--role` tests at the END of the install suite's cap section, but `__tmux_lives_cap_cmd` reads it on EVERY apply — once the user set `cap_role=dim` live, the earlier live-apply assertions compared pal[4] against their pal[2] and failed; guard moved to the top of the section (restore stays at the bottom with `_capv`/`_capw`, AFTER the `tmux_lives_tmux_socket` seam is still pinned). **OPEN (user to decide): `dim`/`text` are BASE-HUE roles by design** (`__tmux_lives_palette` computes them at offset 0 under a `# base-hue roles` comment) — so across all 10 schemes `dim` has exactly **1** distinct value and `text` 1, vs 9 each for `muted`/`accent`. Consequence: at `cap_role=dim` the scheme (`↑↓`) AND vividness (`v`) are BOTH inert for the cap (only the bar color + wheel move it), and the picker's `d` column is 10 identical swatches. Not a math bug — `dim`/`text` were built as palette roles for Phase B whole-bar theming (where they'd color different ELEMENTS), and v2 made the role user-selectable, exposing them as cap choices they were never designed to be. **SUPERSEDED — v3 gradient-map redesign (approved 2026-07-16; **Phase 1 SHIPPED to main @ `698cc74` the same day**, branch merged+deleted; spec `docs/superpowers/specs/2026-07-16-theme-gradient-map-engine-design.md`, plan `docs/superpowers/plans/2026-07-16-theme-gradient-map.md`).** After a 100-agent research run + colorhunt.co/gradient analysis proved geometric hue-harmony is used by ZERO shipped design systems (Radix/Nord/Carbon; Carbon TRIED computational generation and REJECTED it with our exact "colours don't differentiate" symptom), the whole geometric-scheme + `cap_role` engine is being replaced by a **gradient map**: a fixed set of ~7 UI roles each pinned at a LIGHTNESS, sampling ONE hue-arc gradient from the seed (`hue = f(lightness)`). A theme = **seed** + **scheme** (arc width, mono→full-circle) + **phase** (`←→` rotate) + **knobs** (chroma/lightness-range/easing). Cohesion is the monotonic lightness ramp (verified). Removes `cap_role`, the `__tcz_cap_inert` cluster, and all geometric-harmony machinery; renames `cap`→`theme`. Extensively mocked+approved via the visual companion. Phased build (Phase 1 = engine + tmux bar). See memory [[theme-engine-v3-gradient-map]] + [[palette-design-findings]]. **The v2 machinery above stays live/shipped; v3 is opt-in until Phase 2.** **Phase 1 SHIPPED to main @ `698cc74` (2026-07-16; 6 TDD tasks subagent-driven + opus whole-branch review clean + 2 review fixes) — engine + tmux-bar rendering:** pure sampler/roles/palette install-side (`__tmux_lives_theme_*` in conf.d/tmux-lives-install.fish), fragment argv 17-22 seeding 7 role @options, `setup theme <scheme>|list|off [--phase --vividness --shape --ease --range]` CLI with v2-coexistence guards (theme active → `cap_apply_live` no-op, `color --apply` routes through theme; `theme off` → v2 restore), opt-in via universal `tmux_lives_theme` (v2 untouched while off). Phases 2-4 (ShellFish OSC/rename/migration, picker, harmonize modes) still pending. **Pending live smoke (user, after `fisher update`):** `setup theme <scheme>` on the real bar, live role-`@option` retunes, `theme off` v2 restore, light-seed inversion — and the user's WITHHELD colour-placement/ordering tweak is expected AFTER the first live render (role→t + arc direction stay adjustable in `__tmux_lives_theme_roles`/`__tmux_lives_theme_arc`). **Live-smoke waves (2026-07-16 evening, all on main):** `09d899c` fixed the picker draw (a DOUBLE-QUOTED `"$x[(math …)]"` list index is a fish "Invalid index value" ERROR — 2 per draw sprayed stderr into the popup: frame scrolled out, flicker, colorless preview; also full-width title + DECSET-2026 atomic paint; grep-guard bans quoted math-indexes). `73a730e` **explicit polarity + seed-entry UX**: the seed-brightness auto-inversion (L≥0.60→light bar) is DELETED — `--polarity dark|light` (default dark; universal `tmux_lives_theme_polarity`; fragment argv **19**; picker `d` toggle; info line reformatted, no label, worst case exactly 50 cols); the seed contributes HUE ONLY (same-hue seed changes are invisible BY DESIGN — the picker's seed entry now shows a live swatch + extracted-hue readout and says so); seed entry is a raw hex line editor (`__tcz_thp_readchar`, ESC/CSI-disambiguated) replacing the cooked `read` that leaked fish's `read>` prompt. `275d386` **RGB slider seed screen** (user's preferred style): `b` opens 3 channel sliders (`__tcz_thp_slider`, 39-col locked, channel-colored fill) — ↑↓ channel, ←→ ±8 drain-coalesced, hue-on-settle, Enter applies, `t` drops to the typed-hex editor (`__tcz_thp_hexentry`). NB gotchas baked into tests: quoted math-index ban; every drain loop re-asserts `stty min 0 time 0` INSIDE each iteration (readkey's CSI branch resets to blocking — pre-fix the phase drain HUNG); readchar tokens are guarded out of the hex buffer. **Pending: the user's full live smoke** (d/b/t flows) **+ their withheld colour placement/ordering verdict.** **Phases 2+3 SHIPPED to main @ `5e079d3` (2026-07-16; 9 TDD tasks subagent-driven + opus whole-branch review, I1 drain-hang fix pty-verified)** — always-on (unset=mono, `off`=legacy token), v2 engine + `__tcz_cap_*` cluster deleted, migration shim on fisher update (reset-to-mono + vividness/key carryover), ShellFish tabs = tabs role via `__tcz_tab_color`, argv renumbered 12-18, theme picker (`theme-picker` verb, 52×20, bar preview, 5°-phase coalescing) on `M-k`/`M-m k`/`setup theme`. Note the runtime-only live smoke pending. **v3.1 seed-anchored redesign — SHIPPED to branch `feat/theme-seed-anchored` (2026-07-17, from the user's live smoke of the v3 picker; spec `docs/superpowers/specs/2026-07-17-theme-seed-anchored-design.md`; 7 TDD tasks @ `d5a64fe`, final opus review + merge pending):** the bar problem ("always near-black or near-white, seed invisible") is fixed by making the **bar the seed, verbatim** — OKLCH untouched, phase/scheme/vividness never move it; the other 6 roles (sep/tabs/active/windows/cap/text) now CLUSTER around the seed via a gentle per-role ΔL ladder (`__tmux_lives_theme_roles`) with hue/chroma doing the differentiation, and only `text` jumps to the contrast side (L±0.45, C fixed 0.03). `--contrast auto|lighter|darker` (which side companions/text sit on; `auto` = seed L<0.55→lighter) and `--rotate 0-4` (cyclic permutation of the 5 computed support colors, compute-then-permute) REPLACE `--polarity`/`--range` — fragment argv 18/19 are now `themecontrast`/`themerotate`, and `__tmux_lives_migrate_v31` erases the two dead universals idempotently on `fisher update` with a one-line notice. Picker regrown to **layout A, 52×26** at all three open sites: title/tab-chip/preview rows, a bold-titled "adjustments" zone (labeled kv pairs, `__tcz_thp_kv`) for seed/phase/vividness/shape/contrast/rotate/ease, the 10-scheme + off strip, and a 3-row key legend via the new shared `__tcz_legend_row` builder — `a` apply-preview (live `@options` only, no save), `⏎` save (CLI, silenced), `r` reset knobs (seed untouched), `o` rotate, `d` contrast; `Esc`/`q` reverts to the persisted state before closing. The ShellFish tab chip (`__tcz_thp_chip`) renders once at picker-open via the production `__tcz_client_is_shellfish` detection. `__tcz_legend_row` is now shared by the theme picker, both seed screens (sliders + typed hex), AND the session switcher (`__tcz_popup` gained a footer row it lacked: `↑↓ move · ⏎ switch · x kill · esc close`). Both seed-entry screens (RGB sliders, typed hex) grew a big 4-row swatch (`__tcz_thp_swatch`) with hex/hue/L/chroma readouts and "this IS the bar color" copy, replacing the old "hue only" contract line — the seed's L and C now matter. Selection-band `sel-bg` darkened to `#191913` (shared `__tcz_theme` chrome, so the switcher's band darkens too). **Pending: the user's live smoke** (raw-tty feel of a/o/r/d, chip on a real ShellFish attach, 52×26 geometry at all three sites, preview/revert on the real bar) **and a decision on pruning the superseded `2026-07-16-theme-polarity-seed-entry-design.md`** (its polarity model is dead; its raw seed-entry UX survives, re-specified here). **Picker perf + flash + shift-reverse SHIPPED (2026-07-17, `feat/picker-perf-flash`, spec `docs/superpowers/specs/2026-07-17-picker-performance-flash-design.md`):** the picker now sources the theme engine in-process and keeps a knob-state batch cache (key `seed|phase|viv|shape|ease|contrast`, rotate excluded), so keypresses no longer spawn a per-press config-loaded `fish -c` — the measured root cause of felt lag, worst on macOS — and rotate is a free display-side permutation of the cached support colors; a timed `#5fa8e8` change-flash (~0.5s, `stty min 0 time 5`) highlights whichever adjustment field just moved; V/S/E/D/O shift-reverse the cycling keys (vividness/shape/ease/contrast/rotate) backward. Live smoke pending. **Picker anchor row + shake + lit-first (2026-07-19, `feat/picker-anchor-shake`):** an anchor row (❯ indicator, a frozen snapshot of the persisted theme, cursor starts on it) plus a `z` shake key (random scheme/phase/rotate in one press) grew the frame to 52×27 with multi-field flash, and the new `__tcz_thp_litkv` lit-first repaints the kv rows (frame rows 5-8) with the current knob values BEFORE `__tcz_thp_reload` recomputes, called from all 11 knob arms — `__tcz_thp_restore` is gone (the anchor snapshot replaced it); spec `docs/superpowers/specs/2026-07-19-picker-anchor-shake-design.md`; live smoke pending. **Theme v3.2 scheme bar variation SHIPPED to `feat/scheme-bar-variation`** (2026-07-20, spec `docs/superpowers/specs/2026-07-20-scheme-bar-variation-design.md`, 4 tasks): the grid model splits bar/cap/tabs/accents/text onto distinct cells instead of v3.1's single companion ladder — **bar** now takes a per-scheme hue recipe on the seed-depth row (`__tmux_lives_theme_barpos`; `mono` still bar = seed verbatim), **cap** derives from the bar via a calibrated kin-cap family rule (`__tmux_lives_theme_kincap`, fitted from a 4-round visual-companion calibration study, 9/10 acceptance vs 5/10 pre-rule), **tabs** wear the seed verbatim (mono: first ring sample), **sep/active/windows** sample a 5-position accent ring (`__tmux_lives_theme_ring`) that `o` rotate is now the ONLY thing that moves (bar/cap/tabs/text pinned; picker rotpal is ring-fed, display-side), and **text** contrasts the actual bar (not the seed); two behavior shifts worth knowing — `--phase` now steers non-mono bar hues (a real bar knob), and `--contrast` steers the accent ring only (text stays bar-auto); live smoke pending. **Theme v3.3 trio + iTerm2 mirroring** (2026-07-21, branch `feat/trio-tabs-iterm`, spec `docs/superpowers/specs/2026-07-21-trio-tabs-iterm-design.md`): tabs drop the v3.2 seed-verbatim/mono-ring1 rule for **kin-ramp tabs** (`__tmux_lives_theme_kintabs`, user-picked from a live-computed stacked bar/cap/tabs mock) — hue halfway bar→cap (circular), L one 0.16 step toward the bar's own light side when the bar is dark (else away from it), clamped [0.05,0.95], C = the cap's — in EVERY scheme including `mono`, so the bar/cap/tabs trio is harmonious by construction (the old mono special-case, needed only because tabs used to duplicate the bar, is gone); the ✦ mark's home base moves from the cap to the seed verbatim (`@tmux_lives_mark_fg`, both `__tmux_lives_render_fragment` and `theme_apply_live`); `fire`'s bar recipe (`__tmux_lives_theme_barpos`) is corrected from arc-end t `0.05` to **`0.95`** (ΔL −0.03 and cap-chroma-override unchanged) — a spec-authoring bug where the wrong arc end had shipped a BLUE fire bar instead of warm; the claude-coral window coloring (`@tmux_lives_claude_color`, the conditional `window-status-format`/`-current-format`, and the picker preview's coral swatch) is REMOVED so the claude window renders like any other window (the ✦/`(C)` presence indicators are untouched); and iTerm2 clients now mirror ShellFish's per-client OSC treatment via a new `__tcz_client_terminal` (pid → `shellfish`/`iterm2`/`other` from `LC_TERMINAL`, with `__tcz_client_is_shellfish` kept as a thin wrapper) plus `__tcz_emit_itermtab` (OSC 6 tab-color triplet `\e]6;1;bg;{red,green,blue};brightness;N\a`, `\e]6;1;bg;*;default\a` reset for non-hex) wired in beside every existing ShellFish emission site — `__tcz_on_attach`, `__tcz_recolor` (dedup mode included), the color-only heal backstop, and `__tcz_retitle` (title emission is identical for both terminal kinds) — while the non-ShellFish/non-iTerm2 baseline re-source path is untouched; live smoke pending.

**Theme v4 — relationship/placement/mode engine SHIPPED (Phase 1, 2026-07-24, main @ `a49d226`; 8 TDD subagent-driven + opus whole-branch review):** the v3.2/v3.3 DERIVATION (`barpos`/`kincap`/`kintabs`/`roles`/`ring`/`sample`/`arc` + `--rotate`) is DELETED and replaced. A theme = a CURVE selected by a signed **relationship** (hue travel: `mono` 0 / `amber` warm40 / `ember` warm72 / `coral` warm100 / `sage` cool40 / `teal` cool72) × seed **placement** (`bar/tabs/cap/low/high` — where the seed anchors the curve; placing at cap re-anchors the bar to the far hue) × **mode** (`literal` = the placed role renders the seed VERBATIM / `derived` = seed as basis, L/C damped in) + a calibrated endcap **taper** (past a direction-dependent knee 72warm/40cool, capC→0.055 & capL→0.62 so far hues stop clashing with the muted dark bar). Palette signature is now 9-arg `seed relationship place mode phase viv shape ease contrast` → the SAME 7 roles `bar sep tabs active windows cap text`. CLI `tmux-lives setup theme <rel> --place bar|tabs|cap|low|high --mode literal|derived [--phase/--vividness/--shape/--ease/--contrast]`; `--rotate` errors→`--place`. Migration (`__tmux_lives_migrate_v4`) preserves the SEED, erases rotate, maps any retired v3 scheme → `mono/bar/derived` (the user chose DERIVED so their bar VISIBLY DARKENS `#5f772b`→`#44502f` on `fisher update` — intended). Model + 5-round blind calibration (seed #5f772b): magnitude ceiling ~90°warm/72°cool, direction asymmetric→folded into signed relationships, chroma+lightness taper for far caps ("dusty"). Spec/plan: `docs/superpowers/{specs/2026-07-23-theme-relationship-placement-design.md, plans/2026-07-23-theme-v4-engine.md}`. **Theme v4 PICKER (Phase 2) — SHIPPED to main @ `4ac685d` (2026-07-24, 5 TDD tasks subagent-driven + per-task reviews + opus whole-branch review clean, ff-merged from `feat/theme-v4-picker`):** the previously-broken picker (`__tcz_theme_picker` + `__tcz_thp_*` in `functions/tmux-categorize.fish`) is rewritten for the v4 engine, **model B** — the 6 relationships `mono/amber/ember/coral/sage/teal` are the LIST (each a 7-role swatch strip), `place`/`mode` are `p`/`P`/`m`/`M` knobs (place cycles `bar→tabs→cap→low→high`, low/high force `derived`; a switchability seam keeps a future B→A flip cheap — only the `❯`-marker line is B-specific), `←→` phase, `z` shake (random rel+place+mode+phase, randoms captured before quoted `math`), `b` seed (RGB sliders/typed hex), `a` apply-preview (8-arg `__tmux_lives_theme_apply_live`), `⏎` save (config-loaded child `setup theme <rel> --place --mode --phase`, silenced), `r` reset, anchor row LAST + `❯` marker (frozen snapshot at open). viv/shape/ease/contrast are READ-THROUGH/hidden (no knob; keep stored universals). The v3-API breakage is GONE: both picker palette calls are 9-arg (`_reload` list + anchor), `__tmux_lives_theme_ring`/`tmux_lives_theme_rotate`/`__tcz_thp_rotpal` removed, save no longer sends `--rotate` (which errors). Popup shrank **52x27→52x22** (list 10→6, legend 3→2; the draw emits a FIXED 22 rows == `-h 22` in every state — opus-recounted, no conditional `set -a lines`, no top-border scroll) at all 3 open sites + docstring + test pins. Cross-cutting: `p/P/m/M` byte→token mappings added to the SHARED `__tcz_popup_readkey` (proven no-op for the switcher `__tcz_popup` — it has no `case '*'`). Spec/plan: `docs/superpowers/{specs/2026-07-24-theme-v4-picker-design.md, plans/2026-07-24-theme-v4-picker.md}`. **⟶ SUPERSEDED — model B was replaced by the GALLERY picker, then refined; BOTH SHIPPED to main @ `5e70314` (2026-07-25, ff-merged from `feat/theme-gallery-picker`; 5+3 TDD tasks subagent-driven + 2 opus whole-branch reviews clean).** (a) **GALLERY:** the list is no longer the 6 relationships but a curated CATALOG of named `(relationship,place,mode)` schemes — install-side `__tmux_lives_theme_catalog` (28 rows `name|rel|place|mode|default`, 12 flagged default; `__tmux_lives_theme_catalog_default`); opens on the 12, `m` expands to all 28; each row = a recipe rendered live for the seed (**engine/CLI UNCHANGED**; save = same `setup theme <rel> --place --mode --phase`). Windowed scroll (pure `__tcz_thp_window`; fish-float-div bug → `--scale=0`, `-0`-string bug → clamp `-le 0`); linear sel `0..n-1` schemes + `n` off; the `p/P/m/M` place/mode knobs are GONE (baked into recipes); keys `↑↓ · ←→ phase · b seed · m expand · z shake · a apply · ⏎ save`. `setup theme list` renders the catalog. **Catalog weeding pass 2026-07-28 @ `2aad2fe` (28 rows / 12 defaults BOTH unchanged, engine untouched):** driven by rendering all 28 recipes across 5 very different seeds as a faithful status-bar facsimile and asking which fail for EVERY seed (verdict: the engine is sound, only 2 rows out). **CUT `ember deep`/`ember core` (ember|cap|*)** — at cap placement the BAR is the far-travelled role, carrying the full 72° swing at a fixed chroma the taper NEVER touches (proven: the bar hex is byte-identical at capC .115 and .090, so the obvious rescue — dropping ember to the .090 the user picked in an earlier round — provably could not work). A green seed therefore gives a near-miss teal bar that reads as a mistake, while `coral` at 100° survives because a swing that far reads as deliberate — match or contrast, the middle looks broken. Ember is also the ONE relationship sitting exactly ON the 72° warm knee, so it is the sole far scheme that escapes muting entirely. **ADDED the `chip` tier** (`amber chip` default, `sage chip` hidden) = the seed VERBATIM on the tab: the seed previously appeared verbatim at bar (`glow`) and cap (`core`) but NEVER at tabs, 0 of 28 — the gap that started this. Tier order is now `soft glow slate chip deep core` (bar→tabs→cap, derived before literal). NB the pre-existing tests pinned only the 28/12 COUNTS, which this preserves, so they could not catch a composition change at all — hence 8 new assertions, and a cap-placement count pinned EXACTLY (a `>=` bound passed on the pre-cut catalog too). **FOLLOW-UP PASS SHIPPED 2026-07-29 @ `477d532`+`5205635` (user-confirmed by eye: "the proposed schemes look better"):** (a) **EMBER FIXED with ONE constant — warm knee 72→62.** Ember sat EXACTLY on the 72° knee, so `max(0,ΔH−knee)` was 0 and the LOUDEST relationship was the only one escaping the endcap taper, while coral at 100° got pulled to `.055`. That is why every ember scheme read badly. 62 pulls ember under at `capC .090 / capL .645` — independently the same values the user had picked by eye a round earlier. Reviewer-verified SURGICAL: only ember moves (amber/wheat/mint/sage are below their knees; coral/teal already clamp, and coral is byte-identical). Cap `#dea72b`→`#cfa552`: still the only relationship showing orange, no longer neon. NB a hand-check using coefficient `0.001` wrongly suggested coral drifts — the code uses **`0.0015`**; verify against the code, not prose. (b) **`wheat` (−20 warm) + `mint` (+20 cool) added** — user picked 10 of 12 twenty-degree variants over their 40° counterparts, rejecting BOTH cool-20-at-tabs (consistent, not noise). Order is near-first per direction: `mono wheat amber ember coral mint sage teal`. Catalog **28→37**, defaults **12→14** (`wheat soft`/`mint soft` join). No migration risk — new names cannot disturb a stored theme; `__tmux_lives_migrate_v4` leaves them alone (verified). (c) **REGRESSION I shipped and had asserted was impossible:** the commit message claimed the picker derives everything from the catalog so needed no touching. FALSE — `z` shake rolled a HARDCODED bound, and rolled it BEFORE the reload, so it could never see the expanded list; at 37 rows the tail (~9, the core tier) was unreachable while the docstring promised "the full 37-entry catalog". Fixed by reordering (expand → reload → `n` → roll against `n`). **The two existing shake tests string-matched the stale literal, so they PASSED while asserting the bug** — they now pin the derived form + reload-before-roll, plus a guard that fails on ANY literal bound. That guard greps COMMENTS too and immediately caught the explanatory comment quoting the old literal (the comment-defeats-grep trap this repo has hit before). **PICKER UX PASS SHIPPED 2026-07-29 @ `8600895`+`cc10d93`+`1e5f898`** (from live use): (a) **adjustments zone is SPACE-BETWEEN** — seed flush left, phase flush right via a new pure `__tcz_thp_spread` (`<width> <cell>` pairs across w; first left, last right, middles even). Both rows share ONE per-field width so a label sits above its own value. Review caught two latent bugs in it: a min-1-gap clamp OVERRAN w on a tight fit (w=10 with 9 cols rendered 11 — fixed by capturing the repeat into a var and interpolating it QUOTED, so gap 0 is legal; the `-lt 1` floor stays only to catch `math --scale=0`'s `"-0"` STRING), and label/value gaps were computed independently so MIDDLE fields misaligned. (b) **HELD ↑↓ FLOODED** — `←→` had drain-coalescing since 2026-07-19 but `↑↓` never did, so every autorepeat keypress queued its own redraw and the list scrolled for seconds after release. Same pattern applied: a burst collapses to ONE net move (held = faster, stops dead on release). **PgUp/PgDn added** (`ESC [ 5~ / 6~`; the trailing `~` MUST be consumed while the tty is still non-blocking) paging by `WIN`, which is hoisted out of the draw block so dispatch and draw read one value. Multi-step movement loops `__tcz_thp_vismap` rather than clamping inline — one clamp impl, current zone stays out of the walk. Legend `←→ phase` → `⇞⇟ page`. (c) **`m` HOLDS YOUR PLACE** — it kept the numeric index, but expanding interleaves hidden rows so the cursor landed on an unrelated scheme; it now re-finds the selected scheme BY NAME. (d) **PHASE HIDDEN + PINNED 0** (user: "a different seed is the better lever"). NB **saving any scheme now writes phase 0, so a stored non-zero phase RESETS.** `__tcz_thp_litkv` deleted (orphaned — the ←→ arms were its only callers). ⚠️ **CRITICAL bug I shipped and review caught with a repro:** pinning phase meant deleting the init load, but `anch_phase` read the SAME var, so the `current` row (the one idempotent "confirm what I have" action) would have SILENTLY ZEROED a persisted phase. Fixed with a separate `$persisted_phase`. Frame stays EXACTLY 26 (13 singles + WIN 10 + 3 legend). ⚠️ **Grep guards match COMMENTS** — I tripped the quoted-math-index guard TWICE by spelling the banned shape in prose explaining it. Describe the shape, never spell it. **⇒ THE NEXT THEME TASK (design cycle, not a tweak) — `__tmux_lives_theme_accents` IS SCHEME-BLIND:** it declares `capHex` and NEVER references it. sep/active/windows/text are all low-chroma tints of the BAR hue, lightness-only. Verified at seed `#5fb780`: `sep #798c7e / active #c9decf / windows #98b3a0 / text #e0f5e6` are **BYTE-IDENTICAL across all 8 relationships** — a "scheme" moves only 3 of 7 roles. Its own docstring already said "uncalibrated". This is the root cause of the user's "supporting colours attached to the wrong elements" and "colours sticking to particular positions". User's scope: it is the SUPPORTING COLOURS not placement; they REFUSE `rotate` back ("schemes should look good without additional tweaking"); seed-pinning is mere curiosity and **already works via `setup theme <rel> --place bar|tabs|cap|low|high --mode literal|derived`** (low/high have NO catalog rows — CLI is the only route). Fix = make the four participate (relate to the cap, differ from each other in hue) while staying legible on the bar. Reuse the blind-tile calibration method. See memory [[theme-accents-are-scheme-blind]]. **STILL OPEN from this pass:** `--vividness`/`--shape`/`--ease`/`--contrast` are accepted, stored in universals and shown in the picker but are COMPLETELY INERT — verified byte-identical palettes across every value; only seed/relationship/place/mode/phase reach `__tmux_lives_theme_curve`. Decide revive-or-remove. Also unbuilt: a 20° relationship (the engine's closest step is 40°) and seed-literal at 20°, both user-picked in the near-hue round but declined as an engine change. (b) **REFINEMENT:** the `current` scheme is pulled into its own `├─ current ─┤` section reachable ONLY by a new `c` key (out of the `↑↓` flow — `__tcz_thp_vismap` clamps `↑↓` to `0..n`; `c` toggles the current at `sel n+1`); the `❯` in-list marker now gates on recipe **AND** live phase (cursor-independent, clears on any difference); the bottom legend is a cross-row-aligned GRID via the new pure `__tcz_thp_leg <cols> <key desc>…` (**column widths = MAX over ALL rows** — the fix for per-row jitter; the shared `__tcz_legend_row` is kept for its single-row callers); popup **52x22→52x26** (18 static + WIN=8, exact-height, uniform all 3 sites + pins). `c` added to the SHARED `__tcz_popup_readkey` (switcher no-op, regression-guarded). Specs/plans: `docs/superpowers/{specs/2026-07-24-theme-gallery-picker-design.md, specs/2026-07-25-picker-current-zone-legend-design.md, plans/2026-07-24-theme-gallery-picker.md, plans/2026-07-25-picker-current-zone-legend.md}`. **PENDING (USER): (1) CONFIG RESTORE — run `tmux-lives setup theme sage --place high --mode derived`. The cause was misdiagnosed as a killed test; it was actually the CLEAN-PASS `_th_names` hole (see TEST-HARNESS GAP below), now fixed, so the restore finally STICKS — before the fix, any suite run erased it again. Evidence at the time: the live fragment baked sage/high/derived = `bar #544b28 cap #70a84d`, an exact catalog match; `bar_color=#5f772b` intact + unexported. NB that fragment has since been re-rendered under the DEFAULTED place/mode (now `bar #44502f cap #1caf80`), so the fragment is no longer evidence — the restore command above is the record. (2) LIVE SMOKE after `fisher update` — **note the live install is currently at `ca5f6f2`/`4ac685d` (the model-B picker), i.e. it PREDATES the gallery merge; a `fisher update` is required before any of this is testable**: `c` jump + `↑↓`-from-current→off, the 52x26 fit + legend/adjustments alignment, `m` expand, `z` shake, `❯` phase-clear. Deferred cosmetic: residual `anch_*` var naming for the now-`current` row; readkey dead `v/w/s/e/d/o` mappings; orphaned install-side `__tmux_lives_theme_schemes` (0 callers). **TEST-HARNESS GAP — CLOSED, SHIPPED to main @ `30a933d` (2026-07-25, `feat/test-universal-isolation`, 4 TDD tasks subagent-driven + per-task reviews + opus whole-branch review READY-TO-MERGE + a 4-minor fix wave re-reviewed clean; spec `docs/superpowers/specs/2026-07-25-test-universal-isolation-design.md`, plan `…/plans/2026-07-25-test-universal-isolation.md`).** The gap was WORSE than "killed mid-run": `test-tmux-install.fish` wrote `tmux_lives_theme_place`/`_mode` via the real CLI and erased them while BOTH names were absent from the `_th_names` save list, so a **clean, fully-passing run destroyed them every time** (plus unguarded erases of every keybind universal, and `test-tmux-status.fish`'s `prefix_key`/`switcher_key`). Fix: every `tests/test-*.fish` opens with an identical **self-re-exec guard** (md5 `0538ed9cc17766afa9e515812d66f091`) that mints a throwaway dir, points `XDG_CONFIG_HOME` at it, and relaunches the suite under it — **fish binds its universal store at process startup, so the redirect CANNOT be applied from inside a running test**; re-exec is the only mechanism. Load-bearing details: it **fails closed** (mktemp failure → refuse to run, never fall back to the real store); the re-entry condition is `not set -q TMUX_LIVES_TEST_UVARS; or test "$TMUX_LIVES_TEST_UVARS" != "$XDG_CONFIG_HOME"` (an inherited-but-mismatched sentinel re-isolates instead of failing open); mode is preserved across the re-exec via **`test (count $fish_function_path) -gt 0`** — `set -q __fish_initialized` is a TRAP for this (it is itself a universal, so in the child's fresh store it reads unset under plain fish too, misclassifying every run as `--no-config` and silently killing the dual-mode gate); re-exec pins the interpreter with `set -l fish_bin (status fish-path)` (a command substitution in COMMAND position is a fish syntax error — capture into a var first); it sits ABOVE the `gcc` checks in categorize/restore so nothing can `exit` unguarded. Also shipped: `install`/`status`/`generic` now `exit 1` on failure (they printed `FAILED` but returned 0, making the guard's rc propagation inert); 5 isolation-proof assertions in the install suite (the probe-landed-in-temp one is GATED on plain fish — `--no-config` persists no universals at all); a structural check in `test-generic.fish` that greps every `tests/test-*.fish` for `if not set -q TMUX_LIVES_TEST_UVARS` (the ANCHOR LINE, not the bare var name — a comment mentioning the var defeated the first version); and `_th_names` gained the two missing names as belt-and-braces. Proven: full both-mode gate leaves `tmux_lives_*` byte-identical, standalone-run safe, and a `timeout`-SIGTERM kill mid-run (`rc=124`) leaves the real store untouched — the assertion the old save/restore design could never satisfy. Free side effect: `$__fish_config_dir` moves too, so the suite no longer fires OSC colour escapes at live ShellFish clients (`install:275-297`) — but note it also means those blocks no longer prove deployment coverage. Deferred: `/tmp` hardcoded (no `$TMPDIR`), temp dirs leak on SIGKILL, and the isolation assertions hardcode `$HOME/.config` so they pass vacuously under a non-default `XDG_CONFIG_HOME`. **⚠️ ENVIRONMENT TRAP (bit this build): the agent Bash tool runs ZSH, not bash — zsh MULTIOS makes `cmd 2>&1 >/dev/null | wc -c` LEAK stdout into the pipe, so stderr-byte-count checks read garbage (20734 bytes vs the true 0). Wrap such checks in `bash -c '…'`.** **REMAINING v4 follow-ups:** (2) the user's saturated seed → `Cscale`≈1.18 makes non-mono relationships ~18% MORE saturated than the calibration tiles (ember cap `#d67b3d` vs calib `#c97d4b`) — eyeball `setup theme list` after update, reduce Cscale if too loud. (3) only `phase` shapes the Phase-1 curve; viv/shape/ease/contrast are accepted-but-inert. See memory [[theme-engine-v3-gradient-map]] (v4 section) + [[color-calibration-and-scheme-generation]].

**Theme v5 — big-area scheme derivation SHIPPED (2026-08-02, `feat/big-area-scheme`; spec `docs/superpowers/specs/2026-08-01-theme-big-area-scheme-design.md`, plan `docs/superpowers/plans/2026-08-01-theme-big-area-scheme.md`; 5 TDD tasks — `bd685fd` restores the family table + pins the ink, `c1a8f42` rewrites `__tmux_lives_theme_curve`, `0a807e0` adds `place=cap`, `88d01c2` retires `--place low|high`, `7821b61` retiers the catalog):** the v4 curve put every role at a FIXED position on one hue ramp (bar `t=0`, tabs `t=0.42`, cap `t=1`, hue `H0 + sd·t`), so **the endcap absorbed the relationship's ENTIRE travel and the bar absorbed none** — at `place=bar` the bar's hue was exactly the seed's in every relationship. This is provable algebraically, not just empirically: the old curve computed `Hbar = H0 + sd·0`, and `sd·0 = 0` for any `sd` — so the bar sat at the seed hue BY CONSTRUCTION, independent of relationship, past, present, or future. Confirmed against the pre-fix engine (`a49d226`) directly rather than trusting the old paragraph's claim. That is the mechanical cause of the user's "I'm not sensing that the schemes complement one colour more than the others": a scheme was defined by the smallest swath on screen while the two largest areas stayed put. Replaced by **two large areas and a bridge**: the seed anchors one large area (`--place bar|tabs`), the relationship's signed travel separates the OTHER large area from it, and the endcap bridges at **half** that travel, floored at the calibrated kin-cap **family separation** (`__tmux_lives_theme_family`, restored from the 2026-07-20 study the v4 rewrite deleted — warm/earth +40 · olive/green +20 · teal +30 · blue +25 · purple +18 · red/pink +15; blue and red/pink are untested extrapolations). **Depth is FIXED per role** (bar L 0.40, tabs L 0.51, cap ΔL 0.10) — hue differentiates, lightness coheres. `place=cap` survives as the accent-led minority: the cap carries the seed verbatim (literal) or at cap depth on the seed's hue (derived), and the bar is solved BACKWARDS from it, with the family floor evaluated at the SEED's hue rather than the bar's — the code's own comment calls this "a deliberate single pass, not an iteration," since the bar is what's being solved for. **`__tmux_lives_theme_taper` DELETED** — it existed only to mute far-travelled endcaps, which no longer exist; that also retires the ember-knee constant. **`--place low|high` retired**: the CLI's `--place` validation no longer accepts either token (falls through to the generic "invalid place" error, exit 1 — the same failure mode `--rotate` already used), and `__tmux_lives_migrate_v41` rewrites a stored `low`/`high` to `bar` on `fisher update` (idempotent, chained after `_v2`/`_v31`/`_v4`). Catalog **37→35**: bar and tabs are now near-symmetric (bar 8 relationships × derived/literal = 16 rows; tabs 16 minus one dedup = 15 rows, 31 total), cap a 4-row minority ordered last; **the 14 default NAMES are unchanged** (independently diffed against the pre-retiering commit — byte-identical, sorted). The extra cut below the initial 37→36 retiering: `mono|tabs|derived` was removed because at zero travel it rendered byte-identically to `mono|bar|derived` (`mono soft`) — verified at 6 seeds — so the gallery would have shown two rows a user cannot tell apart that merely store different `place` values; `mono` keeps its `bar|derived` (`soft`), `bar|literal` (`glow`), and `tabs|literal` (`chip`) rows. All 35 palettes are now distinct (verified at 8 seeds). One user-visible consequence worth flagging: the retiering drops the catalog row `amber core` (`amber|cap|literal`) — which is the scheme the developer is **currently running live** (`tmux_lives_theme=amber` / `_place=cap` / `_mode=literal` / `tmux_lives_bar_color=#6b9318`, confirmed still intact after all five tasks). The engine still renders that exact combination — the universals are untouched and `setup theme amber --place cap --mode literal` still works — it simply has no catalog row and no picker entry anymore. **`mono` is byte-identical to the old engine at both large areas** (the tabs chroma constant `0.0713` is exactly what the deleted taper produced at zero travel) — the regression anchor pinning `#44502f`/`#5e7239`. **The ink (`__tmux_lives_theme_accents`) is DELIBERATELY UNTOUCHED** per the user ("the ink isn't what needs changing currently") and is now pinned byte-identical by test; it remains cap-blind by construction (the `capHex` argument is declared and unused), and `active` remains a computed-but-never-painted role — `@tmux_lives_active_fg` is still set on every apply but no `window-status-format` string references it (only `@tmux_lives_text_fg` is painted) — both documented as intentional, not bugs for this cycle. Test tolerances are **2°** on rendered hexes, because they quantise to 8-bit sRGB and gamut-clamp; measured directly against the shipped curve at the canonical seed `#5f772b` (replicating the suite's `_oklch_of`/`_dhue` method across all 8 relationships), the worst error is **0.73° on travel, 0.93° on the bridge** — comfortably inside tolerance. **Parked, not fixed — the single known limitation of the design:** at `place=cap` the family floor is evaluated at the SEED's hue (necessarily — the bar it should really be measured against doesn't exist yet), but the bar that actually gets SOLVED can land in a different family band than the seed did. Confirmed by direct computation at seed `#5f772b`: relationship `teal` solves the bar to hue **87.6°** and the cap sits at **125.2°**, an actual separation of **37.6°** — but `family(87.6°)` is **40°** (the `[40,90)` warm/earth band the bar itself falls into), a **2.4° undercut**. Checked the other 7 relationships the same way: all clear their floor (slack ranging ~1.1° to ~20°); `teal` at this seed is the sole exception. This is parked deliberately, not overlooked: the plan explicitly chose the single pass over an iteration, it isn't load-bearing for anything else, and a 2.4° shortfall is a marginal miss on an eyeball-calibrated threshold rather than the "endcap collapses into the bar" failure the floor exists to prevent. Cheap fix if it ever matters in practice: re-floor once at `family(Hbar)` after solving the bar — still deterministic, still not an iteration. `--vividness`/`--shape`/`--ease`/`--contrast` remain accepted-but-inert, unchanged from v4. **`--phase`'s reach is narrower than "rotates the whole palette" implies, and the freeze depends on PLACE as well as `mode`:** it moves the two large areas, but the endcap freezes only when the role its hue derives from is BOTH pinned to the seed AND rendered `literal` — that's `place=bar --mode literal` (the endcap bridges from the literal bar) and `place=cap --mode literal` (the endcap IS the literal anchor, not something read off the bar); at `place=tabs --mode literal` the tabs carry the seed instead, but the bar the endcap bridges from is still derived there regardless of mode, so the endcap FOLLOWS `--phase` normally — that's the entire `chip` tier, including the curated default `amber chip`. In `derived` mode at any placement the anchor is computed rather than pinned, so it carries `--phase` too and the endcap follows (by design, to honour `literal`; the spec's original "added uniformly to all three hues" line was wrong and has been corrected, and the frozen-endcap, tabs-literal-still-moves, and derived-mode-all-move cases are now pinned by test). Full-suite verification: 8/8 `ALL PASS` under both plain `fish` and `fish --no-config`, `test-tmux-install.fish` at **544** / **543** respectively — the 1-count delta is BY DESIGN (one isolation assertion is gated on plain fish, per the 2026-07-25 test-harness-gap fix), not a regression. **Pending live smoke:** whether the far end of the ladder (sage/teal/coral at a saturated seed) is too bold on a large area now that it's carrying real travel, and whether the `teal`/`cap` undercut above is ever visible in practice.

**status-right is MERGED, not assigned — and `update-environment` appends are guarded (2026-08-01, `fix/status-right-preservation`; diagnosed from a macwork handoff at `docs/2026-07-30-handoff-status-right-and-update-environment.md`):** the managed fragment is re-sourced on EVERY reload but was written as if sourced once, which cost two defects. (1) **tmux-continuum schedules its autosave by PREPENDING `#(continuum_save.sh)` to `status-right` — the status bar's refresh IS its scheduler, there is no daemon and no timer.** The fragment's bare `set -g status-right` discarded it, so session snapshots stopped permanently and silently (macwork: 52h, ten live sessions, four running Claude). Recovery was **accidental, not designed**: the fragment emits the tpm run-line AFTER the status-right line, so tpm re-sourced `continuum.tmux` and it re-prepended — a repair skipped entirely whenever continuum's own `another_tmux_server_running` guard trips, which is exactly how macwork lost it while rocket looked healthy. Fixed by INSTALLING rather than assigning: new categorizer verb **`status-right-install <color>`** → **`__tcz_status_right_install`** → pure **`__tcz_status_right_merge <current> <ours>`**, which keeps a foreign prefix and replaces only our own part (so `setup color` still re-bakes the tick without stacking a second copy). The fragment drives it with a plain **`run-shell`, which is SYNCHRONOUS in tmux 3.3a (verified — only `-b` backgrounds)**, so the value lands before sourcing completes. The prefix is kept ONLY if it is nothing but `#(…)` groups and whitespace (`^(\s*#\([^)]*\)\s*)+$`): a looser "contains `#(`" test would weld a user's decorated `#(uptime) %H:%M` on forever, and tmux's DEFAULT status-right (no `#()`) must be dropped or we'd paint the default clock beside our own. Known limits, both pinned by test: a group ends at its first `)`, so a hook whose command contains a literal `)` is dropped; and only a PREFIX is preserved (continuum prepends, so the real case is covered; a plugin using `set -ga status-right` would be evicted). (2) **`set -ga update-environment` appended unconditionally** → 54 copies of each name after ~18 days of uptime. Now guarded per name with `ue=$(tmux show -gv update-environment) && ! printf '%s\n' "$ue" | grep -qx <NAME>` — **`show -gv` prints ONE NAME PER LINE** (array option), so `-x` is the exact test and is load-bearing (a substring match lets `LC_TERMINAL_VERSION` satisfy the `LC_TERMINAL` check and silently drop it); the `&&` makes it **fail CLOSED**, since a bare `! tmux … | grep` takes the pipeline's status and reads "absent" whenever tmux is unreachable, bringing the growth straight back. ⚠️ **HARNESS TRAP found here and worth remembering: a test whose actual-value command substitution calls an UNDEFINED function does not fail — fish aborts the whole statement, `t` never runs, nothing prints, and `test-tmux-categorize.fish` (no pass counter) still reports ALL PASS.** The RED phase needed a deliberately-wrong stub to be real, and every new builder now carries a `functions -q` existence assertion; grep guards are paired with positive counts for the same reason. Opus review found and I fixed: the vacuity hole, the fail-open guard, and the weld-forever heuristic. **VERIFIED LIVE 2026-08-02 on macwork:** continuum's hook survived a full `source-file ~/.tmux.conf` — the exact operation that used to evict it — and the automatic 15-minute cycle resumed, firing on the boundary after a 5-day gap. NB the failure was invisible because **continuum backgrounds its save, discards ALL output, and stamps `@continuum-save-last-timestamp` UNCONDITIONALLY**, so the timestamp advancing proves nothing; only the resurrect FILE LIST does. `@continuum-save-interval` proves nothing either — our own fragment sets it. See [[continuum-silent-failure-and-macwork]].

**Picker second list + layout revision + input fixes (2026-08-01, `feat/picker-second-list`; spec `docs/superpowers/specs/2026-08-01-picker-second-list-design.md`, plan `docs/superpowers/plans/2026-08-01-picker-second-list.md`):** `off` LEFT the scheme list — it was drawn OUTSIDE the scroll window, so it sat on screen wherever you scrolled ("I don't need to be reminded of it while I'm scrolling"); ordering was never the problem, visibility was, and conceptually it is a state of the install like `current`, not a catalog entry. The two now share an **untitled** section as a **second selectable list**: **`⇥` (Tab, byte `0x09`, added to the SHARED `__tcz_popup_readkey` — proven a no-op for the switcher, which has no `case '*'`)** moves the cursor between the lists and `↑↓` clamps within whichever has focus. Selection is now `focus` (`list`/`state`) + `sel` (`0..n-1`) + `sel2` (`0`=current, `1`=off), replacing the linear `0..n-1`/`n`=off/`n+1`=current-reachable-only-by-`c` model; **`c` is retired** (a key meaning "current" that lands on current, from which you arrow to off, promises one thing and does another). New pure builder **`__tcz_thp_staterow <w> <cells> <name> <label> <selected> <live>`** replaces `__tcz_thp_off_row`: swatch, name left, role label **flush right**. **`current` is a LIVE-STATE readout** — bold `brand` only while the persisted theme is what is actually applied. `previewed` is three-valued but the mapping is **not** a clean 1:1 with "what you're looking at": there are THREE apply-preview call sites now (current / off / a listed scheme), and `off` and a listed scheme SHARE value `1` — both leave the bar in a non-persisted state, so `current` goes unlit for either — while only the current row's own site sets `2` (still live: the persisted theme really is on the bar). **The movement rewiring and the fix for its fallout are the same task, not two:** the initial wiring moved `↑↓`/`⇥` to the `focus`/`sel`/`sel2` model but left THREE consumers still reading the retired linear range — the top preview bar's palette lookup, `case a` (apply-preview), and `case enter` (save) — which the movement change made permanently unreachable (`sel == n` / `sel == n + 1` can no longer occur once `__tcz_thp_vismap` confines `sel` to `0..n-1`). Left alone, tabbing to current/off and pressing `a` or `⏎` would have silently fallen through to whatever the SCHEME list's `sel` happened to hold — saving or previewing the WRONG theme with a fully green suite the whole time, since nothing yet asserted the second list's OWN apply/save paths. Caught and fixed within the same task (all three now branch on `focus`/`sel2`); a catch-all guard, **"no surviving reads of the retired sel range"** (`sel -eq $n` / `sel -eq (math $n + 1)`), now covers the class instead of enumerating call sites. Fixing `case a` also incidentally repaired a **latent bug that predates this plan**: `off`'s apply-preview never actually worked, because the OLD code's `rel=off` default sat behind a `sel -lt $n` guard that — once THIS SAME rewiring confined `sel` to `0..n-1` — was ALWAYS true, so pressing `a` on the off row previewed whatever scheme the cursor happened to be resting on instead of off. The **action-site subprocess guard** (`fish -c` call sites in the picker body, `guard: exactly N action-site subprocesses`) moved **8 → 9 → 8** across the branch: 9 when off gained its own dedicated preview call in the fix above, back to 8 when the seed-screen preview-only fix (below) collapsed two immediate-commit sites into one exit-path commit. **BOTH chevrons are gone** — the current entry in the list is marked by its NAME in brand bold instead (no glyph pretending to be a cursor); the hard-coded `\e[38;5;179m` switcher-yellow went with them. Layout: `SEED` label+value on ONE row (`__tcz_thp_kv`/`__tcz_thp_spread` deleted — kv was the only caller of itself and spread's only caller was kv), zone renamed **`configuration`**, all separator titles in a new **`title`** role (`#d2782a`, brand pulled down ~18%), schemes rule stripped of its subtitle AND its `▲n ▼n` counts (**known accepted cost: those were the only cue that the list scrolls**), and the freed row went to the window — **`WIN` 10→11**, frame still EXACTLY 26 (15 static + 11). That count is now **proven DIRECTLY, not by grep**: every `__tcz_thp_*` row builder the draw loop calls (`ln`/`zsep`/`leg`/`row`/`staterow`/`window`/`tabstrip`/`preview`/`seedrow`/`cells`/`band`) is a TOP-LEVEL function, not nested inside `__tcz_theme_picker` — so the real per-iteration draw block (cursor-row palette lookup through the bottom border) can be extracted VERBATIM from the live file and `eval`'d against real state, no reimplementation of the row-building logic. `eval`, not `source`: fish's `source` opens its OWN local scope (a minimal repro confirms a `set -l` inside a sourced file does not survive the call returning), so `set -l lines` inside the extracted block would vanish before it could be counted; `eval` runs inline in the calling scope, which is what lets `$lines` survive. Checked against 10 states — window top/bottom at both catalog sizes, list vs second-list focus, current vs off selected, all three `previewed` values, the persisted-theme-off/empty-`anchpal` edge case, and an active seed change-flash — all 26, and the harness was verified sensitive (a deliberately injected extra row was caught as 27 across every state, then reverted). This is the strongest guard on the file; future picker changes should extend this direct check rather than fall back to counting `set -a lines` sites or reading the `WIN` literal. Swatches are now **`▇` (U+2587) in the role colour, not a filled cell**, leaving one eighth clear at the top so stacked strips stop merging (`__tcz_thp_cells`/`__tcz_thp_band`; the selection band shows through the gap for free, since the draw already re-asserts `sel-bg` after every reset). **Esc restores the SEED:** the RGB-slider and typed-hex screens are **preview-only** and `⏎` commits — they used to call `setup color` immediately, so the original was gone before Esc could restore it, and since every role derives from the seed the scheme looked unrestored too (ONE root cause, both halves of the report). **No engine change was needed**: `__tmux_lives_key` reads `set -q` then `$$name`, so a `set -g tmux_lives_bar_color` inside the existing `fish -c` child SHADOWS the universal for that child alone — enough to preview an uncommitted seed on the real bar. Chosen over capture-and-restore because that writes the seed twice and every fragment write re-sources `status-right`. **Held `↑↓` now rate-limits with DISCARD:** the 2026-07-29 drain SUMMED the burst ("a held key scrolls FASTER") so intermediate positions were never drawn and release landed on the accumulated total, not the last row you saw; the drain stays (it is what prevents the redraw backlog) but swallows queued arrows without counting them — one row per render cycle. Pages still coalesce. ⚠️ The drain-hang guard is load-bearing: `stty min 0 time $gap` MUST be re-asserted INSIDE the loop, because readkey's CSI branch leaves the tty blocking. **Pending live smoke:** the `⇥` feel, the `▇` gap on a real Nerd Font, the `current` label lighting/dimming as you preview, and Esc restoring a changed seed. **⚠️ THE T8 IMPLEMENTATION OF THAT ARROW FIX STALLED, and only the whole-branch review caught it (fixed @ `03cafbd`)** — `case up down; set gap 1` escalated the drain's poll timeout to 100ms, and the drain loop only breaks when a poll TIMES OUT, so while autorepeat arrives faster than 10/s it never yielded to a redraw at all: a held key moved NOTHING until release, then exactly one row. Measured at the real ~61ms redraw cost, holding ↓ for 2s moved **2 rows out of 37** — discard-instead-of-sum had turned overshoot into near-total stall. Fix = drop `set gap 1` from the ARROW arm only (pages keep it, so coalescing and the anti-backlog property are unchanged); re-verified on a real pty at 1→21 rows/2s @60ms, release stops dead, single presses instant. **All nine per-task reviews missed this** — it is only visible as timing behaviour, which no grep-shaped assertion can see. The final wave also fixed 6 minors @ `ab024ce` (reanchor `anchpal` on seed change; `islive` two wrong cases; the current row naming the catalog entry; a genuinely vacuous `switcher-yellow` guard; a mis-named drain assertion; `__tcz_thp_staterow` name truncation so an over-long name cannot widen the row past 50 and push the popup to 27 physical rows — the 26-row proof counts `$lines` ELEMENTS, not display columns, so it structurally cannot see that class). **TWO PARKED COSMETIC FINDINGS (user to call):** (1) the current row's catalog-name lookup hits `$recipes` (the DISPLAYED list, 14 rows on open) rather than `__tmux_lives_theme_catalog` (all 37), so 23 of 37 entries — INCLUDING the user's own `teal --place tabs` = `teal slate` — render the bare relationship until `m` expands; reviewer calls the fix a one-liner. (2) `islive`'s case-(b) guard keys off CURSOR POSITION rather than what was previewed, so in an `off` config it can claim live while a listed-scheme preview or uncommitted seed is on the bar. **PROCESS NOTE worth carrying forward: the plan's ASSERTIONS were the weak point of this build, not the implementations — four separate waves of defective ones (vacuous body-greps against a ~700-line function, one UNSATISFIABLE, one self-contradictory PAIR, and one whose sample COMMENT contained the substring its own guard counted). Every implementer from T5 on was told to treat the brief's assertions as suspect and prove each FAILS pre-fix; that caught all of them. Do the same in the big-area build.**

**tmux-sensible dropped + the post-update note reworked (2026-08-05, `4f2a3ee` / `13b0146` / `1c4584f`, direct to main):** TPM loads plugins AFTER our managed fragment, so **`tmux-sensible` silently won every conflict with it** — including overriding our `status-interval 15` with its own `5` on BOTH hosts. Rather than fight the load order, the three settings worth keeping were ported into the fragment and the plugin dropped from the user's `~/.tmux.conf`: **`set -s escape-time 0`** (a SERVER option; tmux's default is **500ms on 3.3a** — real ESC lag in vim and Claude's TUI — though 3.7b already defaults to 10ms), **`set -g focus-events on`**, and **`set -g display-time 4000`** (tmux defaults to 750ms, which flashes our own `M-r` resize hint past unread). Deliberately NOT ported: `aggressive-resize` (risks resize churn with several ShellFish clients on one session) and sensible's key bindings (`send-prefix`, last-window, `C-p`/`C-n`, `R`) — the user chose to let those go; its `history-limit`/`default-terminal` were already no-ops overridden by the user's own config, and its macOS `default-command reattach-to-user-namespace` is a proven no-op here. **Verified live on both hosts: `status-interval` now reads 15.** — **The post-update note was BACKWARDS and is now evidence-driven.** It used to say "run `exec fish` to load the new version" after every update. But fisher's install loop **sources every `.fish` it just copied into the shell that ran the update** (`fisher.fish`, the `for file in … source $file` loop), so THAT shell is already current and restarting it achieves nothing; the stale shells are all the OTHERS, which `exec fish` cannot reach. Two independent signals now, via pure builders: **`__tmux_lives_stale_shells`** (fed by `tmux list-panes -a -F '#{pane_pid} #{session_name} #{pane_current_command}'` — tmux supplies the pid→session mapping for free, so no `/proc`-vs-`ps` portability work; the wrapper `__tmux_lives_stale_sessions` excludes our own pane by asking tmux for `#{pane_pid}` rather than assuming fish IS the pane shell, which it is not from a nested shell) and **`__tmux_lives_removed_functions`** (sourcing an update cannot UNSET a function, so a removed/renamed one lingers even in the updating shell — the ONLY case warranting `exec fish` where you stand; detected by recording shipped names to `~/.config/tmux/tmux-lives-funcs` via `__tmux_lives_shipped_functions`/`__tmux_lives_funcs_path`, seam `tmux_lives_funcs_file`, and diffing next update — **first run has nothing to compare against and reports nothing**). Wording stays `exec fish` everywhere (a personal alias would not travel). Honest limit, not claimed otherwise in the message: **fish shells outside tmux are not counted.** 21 new assertions, 578/577 both modes. NB one test of mine was initially wrong in an instructive way — it asserted the note must never contain "exec fish" when nothing was removed, but it legitimately does when naming the OTHER shells; tightened to check for the "here too" advice specifically.

**THE SHELLFISH CURSOR STROBE, ACTUALLY FIXED (2026-08-06) — and the August "fixed upstream" conclusion was INCOMPLETE.** The flicker returned "in full force" during a watch deploy on macwork. It was NOT a regression and NOT the `cursor-style` mechanism: the discriminator is that a bare ssh shell, Claude *without* tmux, and an **idle** Claude pane inside tmux were all steady — only an ACTIVELY-WORKING Claude inside tmux strobed. That is flicker #2's signature (Claude's own DECTCEM hide/show, ~21 pairs/sec, made visible because frames aren't buffered), not #1's (which fires on ANY redraw, so an idle pane would strobe on the 15s tick alone). **Root cause: `tmux info` reports `Sync: [missing]` for every ShellFish client and `Sync: (string) \033[?2026…` for every Ghostty client — same server, same session, same `TERM=xterm-256color`.** tmux picks synchronized-output support **per client, at ATTACH, from its OWN terminal identification — it never asks the client**, which is the mirror consequence of the very behaviour filed as tmux#5470. So ShellFish implementing DECSET 2026 (verified: a bare-ShellFish DECRQM returns `^[[?2026;2$y`) was **necessary but never sufficient** — tmux simply never found out, and kept writing every frame unwrapped. The Aug-5 "verified fixed" reading was taken under light load and could not distinguish *fixed* from *not currently provoked*; it also predated the 19:42 server restart, though the restart was NOT the cause (the Cellar bottle dates prove 3.7b ran on both sides of it). **Fix: fragment argv[21] `syncterm` ← universal `tmux_lives_sync_terminals` (default `xterm*`, `''` disables) emits `if-shell '<probe>' "set -as terminal-features '<pat>:sync'"`. GATED to tmux ≥3.7** for two independent reasons: only there does the capability map to DECSET 2026 (3.3a emits the older iTerm2 **DCS** form `\033P=%p1%ds\033\\`, which we have no evidence ShellFish reads), and only there can the bug exist (older tmux never answers the DECRQM query, so Claude never enables sync — rocket is immune BY CONSTRUCTION, not by luck). Probe uses `sort -V` so **3.10 > 3.7**, which a numeric compare gets wrong; verified across 3.3a/3.6a/3.7/3.7b/3.10/4.0 and on BOTH GNU and BSD `sort`. **Confirmed live: multiple watch deploys, one of which the user did not even notice happening.** ⚠️ **TESTING NOTES, all learned the hard way here:** tmux **silently accepts an unknown feature name** (`xterm*:notafeature` → no error), so a grep-only guard would ship a dead fix; and **`source-file` returns rc0 with ZERO stderr on a malformed line**, so a parse test is VACUOUS — proven by mutation, then replaced with one that sources the real emitted line (version predicate swapped for `true`) and reads the option back. The behavioural test attaches a REAL pty client via `script` and reads the capability, with a control proving it can report both states; the version gate is driven against stubbed `tmux -V` output. A first mutation attempt (breaking the quote nesting) did NOT fail the effect test — tmux concatenates the adjacent quoted strings into the same working command — so a commented-out inner command was used instead, which every string match still accepts. 13 new assertions, **591/590** both modes. **Still open, unchanged:** the separate, unexplained large-repaint flicker (present on 3.6a where sync is never enabled) survives at a level the user calls tolerable. **Wart carried over:** like `tmux_lives_cursor_style`, this universal has no `setup` CLI setter — both are `set -U` only. See [[shellfish-cursor-flicker]].

**Theme surface cleanup — inert knobs removed, curated rebalance, More Schemes append (2026-08-06, `feat/theme-surface-cleanup`, 9 subagent-driven tasks + per-task review; not yet merged to main):** `--vividness`, `--shape`, `--ease`, `--contrast` are gone — CLI, storage, fragment, and picker. Recorded as "accepted-but-inert" during v4 and never resolved; before deletion the inertness was PROVEN, not assumed: swinging all four to their extremes (`vivid flat cubic darker` against `balanced arc linear auto`) produced **byte-identical palettes on all 35 catalog rows**, pinned by a test written against the pre-change engine (the user had `contrast darker`/`vividness vivid` live on rocket the whole time, both doing nothing). `__tmux_lives_theme_palette` shrank from 9 args to 5 (`seedHex relationship place mode phase`); the CLI now errors on all four flags instead of silently accepting them; `__tmux_lives_migrate_v51` erases the stored universals on `fisher update`, idempotent. **The fragment argv renumber (sync `argv[21]` → `argv[17]`) and its renderer/writer were done as ONE atomic edit, not two, on purpose:** `__tmux_lives_render_fragment`'s positional reads and `__tmux_lives_write_fragment`'s positional writes have to agree exactly, and a mismatch is SILENT — no error, no rc — it just stops emitting the sync-terminal-features line (the fix for the ShellFish cursor-strobe two paragraphs up), with no visible symptom until the flicker comes back. Splitting that pairing across commits would have left a real window for exactly that to happen unnoticed.

**Curated default set rebalanced 8 bar / 2 tabs / 4 cap → 5 bar / 7 tabs / 2 cap** (`__tmux_lives_theme_catalog`, 12 of 35 rows re-flagged, 23 unchanged) — the tab bar is the dominant on-screen surface and was underrepresented in the opening 14-scheme view; all 8 relationships stay reachable from open. **`m` in the picker now APPENDS rather than reshuffles:** pressing it adds the remaining 21 catalog rows below a `More Schemes` header instead of swapping the whole list into tier order, so the curated rows keep their position and scroll context instead of scattering. **No header-skip logic exists anywhere in the movement code (`↑↓`, PgUp/PgDn, `z` shake, the collapse clamp) — deliberate, not an oversight:** `sel` indexes SCHEMES ONLY (`0..n-1`) and can never land on the header; the header is a pure DISPLAY-side insertion, computed from a virtual-row mapping (`vtotal`/`vsel`) at draw time only, so every mover keeps operating on the real scheme list exactly as before the header existed — stepping "past" it falls out of the model instead of being a case each one has to special-case. If a future change ever needs skip logic here, that is a sign the model has been broken, not a feature to add.

**DEFERRED, not touched this cycle: six of the seven palette roles (`bar`/`sep`/`tabs`/`active`/`windows`/`text`) resolve to only 11 distinct values across all 35 catalog rows — only `cap` reaches 19.** So 35 catalog rows collapse to roughly 11 underlying looks, and rows that differ "by a hair" are ones where only the endcap moved. Root cause: `__tmux_lives_theme_accents` derives the four supporting roles from the bar and takes a `capHex` argument it never reads, so whenever the bar repeats, they repeat with it. This is the user's "too samesey" observation given a number, not something the four-knob removal touches (removing dead knobs doesn't fix a derivation that never consumed them) — it needs its own design cycle. See [[theme-accents-are-scheme-blind]].

**Doc-accuracy pass + the picker-seed-section design (2026-08-07, `c92d94f` / `db5cba9` / `dc2df4d`).** The README's theming section described the **v3.2** engine wholesale — kin-cap, kin-ramp tabs, the accent ring, `o` rotate, an anchor row, and the ten arc scheme names (`warm`/`cool`/`span`/`wide`/`aurora`/`sunset`/`fire`/`complement`/`full`) — none of which has existed since v4 replaced arcs with signed relationships and v5 replaced the curve. It also advertised `--rotate 0-4` (exits 1) and a shift-reverse key with no code behind it. Rewritten from ground truth pulled out of the source, and it now **documents the known limitation** rather than omitting it (four supporting roles derive from the bar alone, so six of seven roles resolve to only **11 distinct values** across the 35 catalog rows). `__tmux_lives_theme_schemes` — the last home of the retired arc names, zero production callers, and whose only test asserted the retired list — is deleted; the replacement guard pins the NAMES out of the install source rather than the function. Terminology "gradient-map" dropped from the CLI description and setup help (re-shortened so the framed page still fits 80 cols), and the v3.1 migration notice no longer tells users contrast defaults to auto. ⚠️ **I hit the `$catfile`-class trap in real time writing that guard:** I referenced `$src`, which `test-tmux-install.fish` does not define until ~700 lines later, so it expanded to an empty path and the grep passed VACUOUSLY — caught only because one of two new assertions failed and the other didn't. Seventh instance of that class here; the guard now uses `$plugindir` and says why. **NEXT (spec+plan written, NOT built):** `docs/superpowers/specs/2026-08-07-picker-seed-section-design.md` + `docs/superpowers/plans/2026-08-07-picker-seed-section.md` — 7 TDD tasks folding the seed editor into the picker as a permanent 8-row section, making the popup height a percentage so `WIN = rows - STATIC` (11 visible schemes → **30** on the 62-row client), and regenerating the cursor's scheme live as the seed moves. Rests on measured tmux facts: **a popup taller than the client does NOT clamp — it refuses to open** (`height too large`), `stty size` DOES report a popup's own size (`$LINES`/`$COLUMNS` are not exported), `-w/-h` percentages work and are exact, `-w '#{client_width}'` is rejected, and **no tmux command resizes an open popup**. Also fixes a defect found while investigating "does ESC restore my scheme": the tmux side restores byte-identically (proven across both branches of the cancel guard), but `__tcz_theme_picker` contains **zero** `__tcz_recolor` calls, so the ShellFish/iTerm2 tab colour is only re-emitted by the ~15s tick — it lags in BOTH directions, and after ESC the bar shows your real scheme while the tab shows the abandoned one. **Test-suite hygiene finding (not fixed):** the suites leak tmux **servers**, not just sockets — 2,915 stale socket files and 5 still-running orphaned test servers holding fake `claude` shims were found and cleaned; per [[continuum-silent-failure-and-macwork]] a stray server can suppress continuum's autosave arming when TPM loads, so this is a real hazard, not just clutter. See [[tmux-test-isolation]].

**Picker seed section + height-adaptive frame SHIPPED (2026-08-07, `feat/picker-seed-section`, 6 TDD tasks + this docs task):** the theme picker's popup at all three open sites (the `M-k` fragment bind, `setup theme` CLI, and `__tcz_modal_run`'s `k`) moved from a fixed `-h 26` to `-w 52 -h 85%`, because **a popup taller than the client does not clamp on tmux 3.3a — it prints `height too large` and refuses to open at all** — so any fixed height would eventually break outright on a shorter window, and a percentage is the only geometry that survives every client size. Since the picker can no longer know its own height in advance, it asks: `stty size` reports the popup's own dimensions (`$LINES`/`$COLUMNS` are not exported into a popup), and `WIN = rows - STATIC` where **`STATIC` is 21** — a plan draft briefly miscounted it as 22 before Task 3 landed; the shipped code, its own docstring, and the test harness's parallel `__t9_frame_rows` copy all agree on 21 — the fixed count of top-border/tab-chip/preview/seed-zone/schemes-zsep/second-list-zsep/current/off/blank-zsep/legend×3/note/bottom-border rows. The draw always pads the scheme window with blank framed rows so the frame ends exactly at the popup's bottom regardless of catalog size: at the harness's `rows=52` case (`WIN=31`), the curated-14 list pads 17 blank rows — the ordinary path on a reasonably tall client, not an edge case — and the floor is `rows < STATIC + 3` (24), below which the picker prints a "window too short" message and declines to draw at all. **The seed became a permanent 8-row section** (`__tcz_thp_seedzone`) rather than a one-off full-screen takeover — rows 1-5 are byte-identical whether or not you're editing, only rows 6-8 differ (hex/hue/chroma readouts vs. three R/G/B slider rows), so toggling edit mode (`b`) never resizes the scheme window below it; every row is exactly `w + 2` visible columns, border glyphs included, the same convention `__tcz_thp_ln`/`__tcz_thp_zsep` already used. Edit mode keeps two seed variables deliberately distinct: `editseed` snapshots the seed the moment `b` is pressed — what edit-mode `esc` reverts to, which leaves edit mode without closing the picker — while `anch_seed` remains the picker-*open* snapshot that only the OUTER `esc`/`q` reverts to and that closes the whole popup. **`t` (typed-hex entry) had never been reachable from inside edit mode at all** — byte `0x74` was simply absent from the shared `__tcz_popup_readkey`'s case list, so no `case t` anywhere downstream could ever fire; the fix adds it there (proven a no-op for the session switcher, which has no catch-all `case '*'`), and the typed-hex screen now draws its own border — the popup itself opens `-B`, so an unframed screen used to float on the user's scrollback — and returns to edit mode on exit rather than some other destination. **Live regeneration is deliberately split by measured cost:** one `__tmux_lives_theme_palette` call is ~40ms, a full batch is 310-400ms for the curated 14 and 700-800ms for the expanded 35 — too slow to pay per keystroke — so a `←→` channel move recomputes only the cursor's own scheme immediately, while the remaining visible strips plus the `current` row catch up once input settles (a `stty min 0 time 5` ≈0.5s poll with no further key). The deferred batch is tracked by its own **`seeddirty`** flag, deliberately independent of the pre-existing `flashfield`: `flashfield` is a purely cosmetic highlight, and three sibling arms (`m`/`z`/`⇥`) clear it on their own unrelated keypresses with no idea it once also carried a recompute obligation — coupling the two would have let any of those silently cancel a pending batch. `flashfield`'s only consumer, `__tcz_thp_seedrow`, has been unreferenced since `b6e9ad8` (the same commit that gave the seed its permanent 8-row section and deleted `__tcz_thp_seedrow`'s call site along with the old single-row adjustments zone it painted), so the flag now carries settle-timing only, no display role. **The tab-colour defect fixed here:** `case a` (preview) and `case cancel` (esc-revert) now call `__tcz_recolor` directly with a force emit, so a ShellFish/iTerm2 tab updates the instant you preview or revert a scheme; previously `__tcz_theme_picker` made zero `__tcz_recolor` calls of its own and the tab colour only followed the ~15-second status-bar tick, lagging in both directions — worst case, pressing `esc` left the bar showing your real scheme while the tab kept showing the abandoned preview for up to 15 seconds. `__tcz_thp_sliders` (the old full-screen RGB-slider picker screen) and `__tcz_thp_seedrow` are both now unreferenced by the interactive loop and deliberately left defined rather than deleted.

**⟶ PARTLY SUPERSEDED the same day — the auto-apply half of this paragraph was REMOVED by `fix/drop-autoapply-debounce-seed` (see the paragraph after next). `A`, the `tmux_lives_theme_autoapply` universal, and the dwell apply do NOT ship; `STATIC_IDLE`/`STATIC_EDIT` are 16/21, not 17/22, and the browsing legend is 3 rows, not 4. Everything else here — the area-weighted strip, the `▌` marker, the band, the mode-dependent seed zone, the mode-aware legend, the wired `active` role — still stands.** Picker legibility pass + auto-apply on dwell (2026-08-09, `feat/picker-legibility-autoapply`, 6 build tasks + this docs task). The swatch strip inside each picker row stopped following the engine's internal palette order (`bar sep tabs active windows cap text`) and now orders by measured on-screen area — `tabs(5) bar(4) cap(2)`, a blank tier column, then the trim roles `windows(1) sep(1) text(1) active(1)` — because `tabs` covers roughly **1.8x the area of `bar`** on a real ShellFish client, per the standing principle that ShellFish's tab strip, not the tmux status bar, is the actual optimization target for colour work (cmux gets a veto for "actively bad", not a vote on ordering). `active` earns its first cell in that reordering because it FINALLY paints somewhere: `window-status-current-format` now reads `@tmux_lives_active_fg`, closing a gap where the engine computed and pushed the option every apply while no format string consumed it, so it rendered in zero sessions since the role was introduced. The selection marker flipped `▐`→`▌` (right-half to left-half block) — same width and full height as the swatches beside it, but since the frame's own `│` inks the centre of its cell, the left-half glyph leaves half a column clear on **both** sides instead of hugging the first swatch; a half-*width* swatch cell was designed and rejected because it would have needed a full-height glyph sitting beside seven-eighths-height ones. The seed section's static row budget split by mode — `STATIC_IDLE 17` / `STATIC_EDIT 22`, up from the prior single `STATIC 21` — and the frame-row proof was rewritten to **read both constants out of the source** rather than restate them as literals, closing the exact hole that let the source and the proof drift silently before. Both numbers are one higher than the zone arithmetic (idle seed 3 rows / editing seed 8 rows) alone implies, because the browsing legend's **tenth** pair — `A auto`, added this pass — tips it from 3 rendered rows to 4 (measured: 9 pairs render 3 rows at `cols=3`, 10 render 4), and the editing legend's blank-row pad grew by one to match so both modes cost the same legend height. `A` toggles auto-apply (universal `tmux_lives_theme_autoapply`, default on, `set -U`-only like `tmux_lives_cursor_style` — no CLI setter): settling the cursor on a row for the same ~500ms dwell already shared by the flash highlight and the deferred seed-batch reload now applies that row live, reusing the existing timed poll as a third gate rather than adding a second timer. It is explicitly gated off while editing the seed — dragging an R/G/B channel must never reach the real bar, per **the user's own principle that configuration and adoption are different acts**: a seed edit previews only locally, and only settling on a row (or an explicit `a`/`⏎`) counts as adoption. One fix round was needed after the first cut: a stale auto-apply obligation armed just before `b` was pressed could still fire mid-edit if gated at the arm site instead of the consumer, so the gate lives at the point of application, covering every path into edit mode with one check. A separate fix round, from the STATIC split earlier in this pass, learned that the open-time admission floor has to gate on the **stricter** `STATIC_EDIT`, not `STATIC_IDLE`: a `STATIC_IDLE`-only floor admitted a 20-row popup (what a 24-row client — `80×24`, the canonical terminal default — produces at `-h 85%`), which then overflowed and scrolled its own top border away the instant a first `b` press recomputed `WIN` against the taller `STATIC_EDIT` and went negative; checking the floor against `STATIC_EDIT` up front closes that for both modes before the picker ever opens. Separately, the previously-reported "`⏎` closes the picker" bug turned out to be **a lying legend, not a broken dispatch** — `case enter` already gated correctly on `$editing` and kept the seed change without closing, but the footer never stopped advertising the browsing keys while editing, so the fix was making the legend itself mode-aware rather than touching dispatch at all.

**Auto-apply cancelled outright, seed debounced to 700ms (2026-08-09, `fix/drop-autoapply-debounce-seed`, 2 build tasks).** The user tried `d68deb0` (the auto-apply-on-dwell build above) live and reported it **"wayyyy too much… everything is so lacking in responsivity,"** asking to cancel auto-apply entirely and instead **"debounce the crap out of"** the seed — wait until they stop on a colour as if asking to see it, then apply to the schemes. Three costs had stacked in that build: a 200-400ms real-bar apply firing on every settle (auto-apply itself), a ~40ms palette recompute on every channel keypress (the picker-seed-section live-regeneration path), and the frame's own per-redraw cost, which had grown from 61ms to 122ms. Task 1 removed auto-apply entirely — the `A` key, `tmux_lives_theme_autoapply` (migrated away on `fisher update`), the dwell arming, and the settle branch's apply call — and reversed the arithmetic it had paid for: the legend's tenth pair (`A auto`) is gone, the browsing legend is 3 rows again (not 4), and `STATIC_IDLE`/`STATIC_EDIT` are back to 16/21 (from 17/22). Task 2 killed the remaining per-keystroke palette cost: a channel keypress now does **zero** palette work — the scheme strips stay on the old seed until input actually pauses — with the settle poll moved 500ms → 700ms (`stty min 0 time 5` → `time 7`) so a real stop reads as "show me this colour," not a stutter mid-adjustment. ⚠️ **It does NOT redraw only the seed zone, and the third stacked cost survives untouched:** a keypress still triggers a **full-frame rebuild**, every row re-stringified even though the palettes are unchanged, measured by the whole-branch reviewer at **~90ms per redraw** at 52 rows browsing. That was deliberately out of scope (the plan scoped only auto-apply and the palette recompute) but it is where the remaining lag lives — so if the picker still reads as sluggish after `fisher update`, the target is making the seed-edit redraw touch only the seed-zone rows instead of rebuilding the frame, NOT further debouncing. The discriminator that matters here is a call **count**, not a grep: a mistaken fix could drop the batch reload while still recomputing the cursor's own row inline, so the test wraps a counter around `__tmux_lives_theme_palette` itself and asserts 0 calls per keypress, proven to fail (count 1) before the removal. Nothing in either task touches the real bar on a settle — only `a` and `⏎` still do; configuration stays private, adoption stays explicit. **Gate as shipped: 8/8 suites `ALL PASS` in both modes, `test-tmux-install.fish` 639 / 638 (the 1-count delta between plain and `--no-config` is BY DESIGN — one isolation assertion is gated on plain fish), `test-tmux-categorize.fish` 893.** Measured popup geometry, verified with a real pty rather than computed: a client of 28 rows yields a 23-row popup and is refused, 29 yields 24 and is admitted, 62 yields 52 — so the visible-scheme counts are 36 browsing / 31 editing, and the user-facing floor is "below 29 client rows the picker will not open." **Everything about this pass is pending the user's live smoke; the whole point of the second build is a feel change no in-process test can measure.**

**Picker responsiveness, seed zone, and default list SHIPPED (2026-08-14, `feat/picker-responsiveness` -> main @ `df55935`; spec `docs/superpowers/specs/2026-08-13-picker-responsiveness-and-layout-design.md`, plan `docs/superpowers/plans/2026-08-13-picker-responsiveness-and-layout.md`; 8 TDD tasks subagent-driven + per-task reviews + opus whole-branch review + one fix wave).** The user's live verdict on `cec4393` was **"kind of unusable unless you're really slow with it"**, with two symptoms: the list trailing their fingers on a quick reversal, and the seed editor going "dark until you let go" under a held key. **The frame cost was measured, not assumed, and the record was badly wrong: 128 ms browsing curated, 266 ms expanded, 142 ms editing** at the real 62-row-client geometry — not the "~90 ms" CLAUDE.md and the previous whole-branch review both carried. That figure predated the popup going `-h 26` -> `-h 85%`, which took visible rows 10 -> 36; nobody re-measured. **The cost is the NUMBER of fish command substitutions, not any one builder**: a function call inside `(…)` costs **0.108 ms** against **0.0057 ms** for a plain call — **19x** — and `__tcz_thp_cells` runs ~17 of them per call, making it **96% of a scheme row**. **Partial repaint was considered and rejected**: it optimises EMISSION while the cost is CONSTRUCTION, so rebuilding 35 rows to emit 2 still pays the full price; memoizing the pure builders buys the same "only two rows changed" win by not doing the work, and leaves whole-frame emission, the synchronized-output model and the 26-row frame proof untouched. Shipped: **row cache** keyed `<index>_<selected>_<current>` (a cursor move dirties exactly 2 of 35 rows), **static-row caches** (preview/tab chip/seed zone/legend/both state rows), **swatch memoization**, and `anchcells` — a call site that rebuilt every redraw whenever a theme was set and which **no task in the plan covered** until a reviewer read the whole plan to establish it. **All caches share the `__tcz_cc_`/`__tcz_rc_`/`__tcz_sc_` prefixes behind ONE clear helper called from ONE place (`__tcz_thp_reload`)** — that single invalidation point is what makes a bare-integer row key legal, and it is now pinned by a guard counting palette-array writes inside the picker body vs inside reload (6 == 6; 7 != 6 under mutation), plus a companion pin confining `anchpal` writes to `__tcz_thp_reanchor`. **Measured result at the user's geometry: 167 ms -> 35 ms whole-frame (4.8x).** NB the spec's **61x is the ROW LOOP** (135.3 -> 2.2 ms), not the frame — the residual ~35 ms is the un-memoized rest of the draw (~50 `__tcz_thp_ln` calls, SELBG padding, `__tcz_strip_sgr`, `__tcz_popup_truncate`, the final printf) and is the next target if it ever matters. **Both reported symptoms verified fixed on a real pty**, not by reasoning: reversal fidelity now holds down to a **30 ms tap interval** (pre-branch it lost taps at 80 ms), and the edit-mode held-key post-release tail went **46 frames / 4.81 s -> 1 frame / 0.20 s**. Input is now ONE rule on all three held-key paths — **discard, one step per frame**: the list already did this, edit-mode `↑↓` (channel select) had **no drain at all** (the "goes dark" defect — every autorepeat byte got its own full frame, unbounded, with `chan` clamped 1-3 giving the "jerk a couple times"), and the `←→` slider **summed** the burst so intermediate values were never drawn and there was nothing to stop on. **`--place low|high`-era drain trivia that still bites: `stty min 0 time 0` must be re-asserted INSIDE every drain loop on the very next line** (readkey's CSI branch leaves the tty blocking) and **the arrow poll must never escalate its timeout** — the loop breaks only on a poll TIMEOUT, so a ~100 ms gap never times out while autorepeat outpaces it. **Seed zone:** the colour block grew 2 -> 3 rows with the hex moved off the right and **centred inside** the block in a contrast colour derived from `$hex` — NOT from the picker's `seedfg`, which the spec named and which turned out to be **dead** (four occurrences, all assignments, zero reads), so following the spec literally would have painted a stale colour through every slider drag and gone unreadable the moment one crossed the light/dark crossover. Consequences: `STATIC_IDLE` **16 -> 17**, `STATIC_EDIT` **21 -> 22**, admission floor **24 -> 25 popup rows = 30 client rows** (measured on a real pty: 29 client -> 24-row popup, refused; 30 -> 25, admitted). **List:** opens on all **35** schemes (`expanded` now inits to 1), the `More Schemes` rule stays as a divider marking the curated 14, and **`m` reverses to COLLAPSE** (legend `m more` -> `m curated`; the row count is a modulo over PAIR count, not string width, so a longer label was safe — but verify that, since an `A auto` pair once tipped this legend 3 -> 4 rows and silently invalidated `STATIC_IDLE`). At `WIN` 35 vs 36 virtual rows the list now scrolls by exactly one row on the user's own client; the header is a display-side insertion and `sel` indexes schemes only, so **no mover needed header-skip logic** — if a future change seems to need it, the model has been broken. **Catalog reordered seed-literal first** (`glow chip core soft slate deep` = **18 literal then 17 derived**), a PURE reorder proven three ways (empty sorted-set diff, same 14 names still flagged, within-tier order untouched). NB the plan specified `glow soft chip slate core deep` and **contradicted its own assertion** ("every literal precedes every derived", which that order fails); resolved toward the user's stated purpose of comparing literal vs derived as groups. **THE PROCESS RECORD, because it is now unambiguous: nineteen plan defects were found across eight tasks, every one by an implementer or reviewer, and NONE reached `main`.** Shapes worth recognising: an assertion that was **outright backwards** (it counted drains across the whole case arm, so it passed pre-fix and would have FAILED post-fix — an implementer trusting it would have deleted the working drain); **awk ranges that start and end on bare `case` lines**, which fish rejects outside a `switch` while **`eval` still returns status 0**, so the body never runs and every assertion under it reads a plausible zero; a self-review table that **claimed** the reload invariant was covered by a step that only wired the clear; and repeated **vacuous guards**. **The single most valuable technique remains mutation**: Task 2 shipped six correct cache keys whose tests passed hand-written keys, so four separate mutations of the real call sites each reintroduced a visible bug (the `off` row vanishing, the seed readout freezing, a 53-row frame in a 52-row popup) with **922 assertions green**; Task 6's contrast fg — the headline of its own task — survived three mutations at **961 ok / 0 FAIL**; and the reload invariant's violation is **100% invisible both on screen and to a 652-assertion gate**. Reviewers were told to reproduce rather than read, and every one that found something real did so by mutating or rendering. **Gate as shipped: 8/8 `ALL PASS` both modes, `test-tmux-install.fish` 652 / 651** (the 1-count delta is BY DESIGN — one isolation assertion is gated on plain fish), `test-tmux-categorize.fish` 976. **Deliberately NOT done:** the drain is still direction-blind (`case up down` discards either token) — the spec justified this on a "single-digit ms" premise which measurement showed to be **31-35 ms, ~4x wrong**, but the conclusion survives since reversals land correctly well past any human tap rate; corrected in the spec rather than carried forward. **Pending the user's live smoke after `fisher update`:** whether the picker now reads as responsive at 35 ms, whether 700 ms is still the right seed debounce at the new frame cost, the 3-row seed block with the centred hex on a real Nerd Font, and the all-35 default with `m` collapsing.

**Stale shells auto-refresh after an update SHIPPED (2026-08-14, `feat/shell-autoreload` -> main @ `3f807bd`; spec `docs/superpowers/specs/2026-08-14-shell-autoreload-design.md`, plan `docs/superpowers/plans/2026-08-14-shell-autoreload.md`; 4 TDD tasks subagent-driven + per-task reviews + opus whole-branch review + one fix wave).** `fisher update` sources the new files into the shell that ran it; every OTHER running fish kept the old definitions and the note told the user to run `exec fish` in each — impossible in a pane running Claude, since **the pane's shell is Claude's PARENT, so `exec` would kill it**. The user asked whether the updater could do it and anticipated the objection themselves ("if it uses send-keys I suppose that wouldn't work for claude sessions"). Correct: `send-keys` types into the pane, so in a Claude pane it lands in Claude's input box. It IS gateable on `pane_current_command` (which `__tmux_lives_stale_shells` already parses) but two problems survive the gate — a half-typed prompt gets `exec fish` appended to it, and `pane_current_command` reads `fish` for a running fish SCRIPT too. **Shipped instead: a `--on-variable` universal event.** `_tmux_lives_post_update` sets `tmux_lives_reload_token`; `__tmux_lives_shell_reload` fires in every already-running shell and re-sources the two `conf.d` files **in place** — no `exec`, nothing typed into any pane. Only re-sourcing can refresh a shell without replacing it, which is exactly why it is the design. **Only the two `conf.d` files are re-sourced, NOT `functions/tmux-categorize.fish`, and the reason is worth keeping:** the categorizer is only ever invoked as a script (`fish --no-config $tmux_categorize_script <verb>`, zero bare-name call sites), so a running shell cannot hold a stale copy — and separately, **fish does NOT reload an autoloaded function when its file changes on disk** (verified), so a shell that HAD autoloaded it would keep the old definitions for life. Re-sourcing `conf.d/tmux.fish` into a live shell is safe by construction: `__tmux_should_autostart` bails on `set -q TMUX`, and the stack-trace guard at `conf.d/tmux.fish:392` trips because the handler is a function — the whole-branch reviewer proved the autostart hijack unreachable with a tripwire and a sensitivity control. Supporting pieces: **`__tmux_lives_shell_is_idle`** (tty foreground-pgid vs the shell's own; `/proc/<pid>/stat` on Linux, `ps` fallback; **returns FALSE with no controlling tty on purpose** — "unsure" must mean "do not print"), a **digest gate** (the event fires even on an UNCHANGED value, so the emitter compares a fresh `__tmux_lives_digest` against the stored token and sets only on a difference; fisher re-fetches every time, so no-op updates are the common case), the **`tmux_lives_autoreload` opt-out** (`set -U` to 0; honoured by BOTH the handler and the note's `autoreloaded` computation), and a **4th trailing optional arg to `__tmux_lives_update_note`** that reports the refresh instead of advising `exec fish`. **The note keeps the old advice in two cases and both are real:** a removed/renamed function (sourcing cannot UNSET one, so every shell still needs a restart) and the FIRST update carrying the feature (already-open shells have no handler yet — distinguished by whether the token existed before). **⚠️ TWO MEASURED CLAIMS IN THE ORIGINAL SPEC WERE FALSE, BOTH FROM ONE FLAWED PROBE, AND THE SHAPE IS THE LESSON.** The probe registered its handler in `conf.d`, so the separate `fish -c 'set -U …'` SETTER process loaded the same handler and fired it on ITSELF — its firing was miscounted as the shell's. That single mistake produced (1) "fires while a foreground child runs, so a Claude pane refreshes immediately" and (2) "one `set -U` fires it twice, so idempotency is mandatory". Re-measured with **per-pid logging**, both collapse: a shell receiving a FOREIGN notification defers dispatch until control returns to its main loop (so a Claude pane refreshes when Claude EXITS), and each shell fires exactly ONCE. **Log WHICH PROCESS did a thing, not merely that it happened.** Idempotency is still required, for a real reason instead: the handler re-sources the file that DEFINES it, re-registering its own binding mid-dispatch (verified tolerable — the running invocation completes with the old body). **And the correction was itself over-broad, caught by the whole-branch re-review:** a `set -U` in the SAME shell fires its own handler **INLINE, mid-statement** (verified, same pid) — only foreign notifications defer. So the updating shell re-sources redundantly and prints its notice from inside the fisher job. **A COLLISION THAT WOULD HAVE SHIPPED SILENTLY:** the spec, plan and brief all named the handler `__tmux_lives_reload` — which ALREADY EXISTS at `conf.d/tmux-lives-install.fish:282` ("source the tmux config into a running server"), called by `__tmux_lives_write_fragment:301`. **Fish redefines with no warning**, so every `setup color`/`setup theme` would have quietly stopped reloading the tmux config, with no error and no symptom until the bar went stale. Caught by Task 2's implementer, who proved it by mutation before writing code; renamed to `__tmux_lives_shell_reload`. The memory [[fish-function-conventions]] says explicitly to check a new function name against the existing ones and cites a prior collision — it was loaded and not applied. **Add a name-collision check to plan pre-flight.** **A REAL HOST-STATE LEAK, PRE-EXISTING AND UNDETECTED FOR WEEKS:** `__tmux_lives_funcs_path` falls back to real `$HOME`, which the suites' `XDG_CONFIG_HOME` re-exec guard does NOT redirect, so any test reaching `_tmux_lives_post_update` unseamed truncated `~/.config/tmux/tmux-lives-funcs` to 0 bytes on EVERY run — wiping the ~209-name baseline and silently defeating removal detection, which is the one thing that still needs `exec fish`. Fixed by seaming **seven** call sites (three of them inside Task 3's own trigger block) and pinned by a committed md5 bracket that fails closed. The whole-branch review then found the **same class again** in `$XDG_DATA_HOME`: a reader pane started as plain `fish -i` was writing every `send-keys` into the user's real `~/.local/share/fish/fish_history` (99 entries measured). **Gate: 8/8 `ALL PASS` both modes, `test-tmux-install.fish` 703 / 702** (the 1-count delta is BY DESIGN). The whole-branch reviewer ran a **10-mutation battery** against the production code — drop the opt-out, the idle gate, the carrier hand-off, the carrier erase, the note's opt-out, the wrapper's 4th arg; token to `set -g`; stop re-sourcing `tmux.fish`; clobber the pre-existing `__tmux_lives_reload`; drop `status is-interactive` — **all ten caught** — and verified the full chain on real processes with a fisher stub matching fisher's actual copy/source/emit sequence: bootstrap V1->V2, steady V2->V3, and a BUSY pane held `sleep 12`, did NOT refresh during it, then refreshed and printed once the moment the sleep exited. **Known and deliberately not fixed:** the `tmux_lives_tmux_socket` seam is BROKEN in `__tmux_lives_stale_sessions` (`set -l tm "command tmux -L $sock"` then `$tm list-panes` — fish does not word-split, so the whole string becomes the command name); pre-existing, isolated to that one function, every other seam site uses the correct `if set -q` form. **Pending the user's live smoke after `fisher update`** — and note the FIRST update after this ships cannot reach already-open shells, by construction.

**Hygiene pass — the socket seam and the socket leak (2026-08-14, `fix/seam-and-socket-hygiene` -> main @ `3e40826`).** Two defects the shell-autoreload whole-branch review found and deferred. **(1) The `tmux_lives_tmux_socket` seam was broken in `__tmux_lives_stale_sessions`, and it was broken TWO ways — both of which look correct in fish, so record both:** `set -l tm "command tmux -L $sock"` then `$tm list-panes` does NOT word-split, so the whole string becomes the command name (`Unknown command: 'command tmux -L …'`); and `set -l tm command tmux -L $sock` builds a correct LIST but fish refuses a **keyword arriving via expansion** (`The expanded command is a keyword.`). **There is no variable form that works** — which is why every other seam site in the file (`:698`, `:916`, `:1264`) spells the command out in both `if/else` branches. Now this one does too, for **both** tmux calls in the function (the second, `display-message`, was missed on the first pass and produced a third distinct error, `The expanded command was empty.`). NB the accompanying test needs **two** sessions on the probe server: with one, `display-message -p '#{pane_pid}'` on an *unattached* server returns that sole session's own pane pid, so the function correctly excludes it as "ours" and reports nothing — which reads as the seam failing when it is working. **(2) `test-tmux-install.fish` started ~20 `-L` servers per run and killed every one, but killing a tmux server does NOT unlink its socket file** — they had accumulated past 1500 in `/tmp/tmux-<uid>/`. An end-of-suite sweep now removes them, scoped to the run's own `$fish_pid` so it cannot touch another run's files or the user's `default`, and pinned by a self-checking assertion (proven: reports 17 leftovers when the sweep is removed). The sweep **fails closed** first — if `$fish_pid` were ever empty the glob collapses to `**` and would match every file in that directory including `default`; `$fish_pid` is a fish-protected special variable so it is not currently reachable, but the guard encodes the invariant. Gate 8/8 both modes, `test-tmux-install.fish` **708 / 707**. **Known and deliberately deferred: `test-tmux-popup.fish` carries a hard-failing WALL-CLOCK assertion that flakes under load** (it failed once in this session's merge gate at load 3.27, then passed three consecutive re-runs, and references nothing the branch changed). It has now bitten twice and it trains a "re-run until green" reflex — it deserves either a widened bound or a non-fatal warning, and that is the next hygiene fix.

## claude-mem history

This project was extracted from `~/.config/fish`; its development history (through
2026-06-17) lives in claude-mem under the project label **`fish`**, not `tmux-lives`.
When searching claude-mem / mem-search for prior work on this system, also query
`project: "fish"` (terms like "tmux", "auto-tmux", "categorize", "shellfish",
"resurrect"). New observations from this repo are tagged `tmux-lives`.

## Migrating the Claude context here (run at switch-time)

The conversation history + file-based memories still live under the
`~/.config/fish` namespace. When you're ready to make this repo the primary working
dir, replicate that context with the migration skill (one command; reversible):

```bash
rm -rf ~/.claude/projects/-home-bitsaver-workspace-tmux-lives        # clear the empty /add-dir stub
bash ~/.claude/skills/migrating-project-directories/scripts/claude-code-migrate.sh \
     --mode replicate ~/.config/fish ~/workspace/tmux-lives
```

Do this **last** (right before switching), so it captures the latest state rather
than a stale pre-extraction snapshot. `~/.config/fish` is left fully intact.
